# _MASTER-WORKLOG.md — YouTube Media Flywheel

> ## ▶ NEXT SESSION — START HERE (Sacred Dawn: Parts I-III SHIPPED · motion CLOSED · batching Parts IV+ while v2 builds)
>
> **30 JUL UPDATE — supersedes the STATE/TASKS blocks below where they conflict:** Part III watched in Studio: **best film yet.** Motion thread CLOSED (Laws 22-23 in `_CANONICAL.md`; duration patch field-confirmed at kling-12; fuzzy move law visibly varied on screen; Worry closed by kling-coverage explanation — no fourth bug). New composition-archetype law + variant-bank spanning rule banked and tooled into `chop_river.py` after two named repetition complaints (seated-authority ~80 beats across four throne tokens; reader-by-fire 22+). **Strategy: batch through ≥half the slate with forward-scheduled publishing (`run_batch.py --publish-start --publish-interval-hours`) for daily coverage while v2 builds without ship pressure.** Part IV (Giants) authoring underway under archetype discipline; Part V follows. v2 status: design banked (schema draft 1, decommission map, scope+backlog), build not started; stages 1+2 first, blocked only on `align_with_whisper.py` upload.
>
> **LOAD (the standard ritual):** `_CANONICAL.md` (v5) · this worklog · `the-bible-they-burned-series-slate.md`. The old load set (_PIPELINE-CANONICAL / _Synthetic2 / STARTUP_PACK) is DEAD. Deep narrative of the 28-29 Jul pivot: `SESSION-NOTES-2026-07-28-29-river-first-enoch-redo.md`. Deep narrative of the 29 Jul evening Part II session: `SESSION-NOTES-2026-07-29-astronomy-book-enoch-part-ii.md`.
>
> **STATE (29 Jul, late evening):** Part II (Astronomy Book) launched earlier this evening at kling-40, running unattended (`astro2b`) — **not yet watched, still the first task below.** Part III (Parables of Enoch / Son of Man) authored, audited, compiled, and machine-gated in a SECOND session the same evening, using a new generalized tool (`chop_river.py`) built and proven in that same session — **not yet launched; handoff block ready, waiting on Peter.** Part III deliberately held to the standing kling-12 (not continuing Part II's kling-40 hedge), since that hedge's outcome isn't confirmed yet — don't let Part III's launch imply the hedge worked; it hasn't been checked.
>
> **THE FIRST TASKS, in order:**
> 1. **Watch Part II (Astronomy Book) in Studio, full runtime** — do floor beats beyond kling's front-40 show real, correctly-scaled motion (the actual test of the evening's duration-order patch); does the cosmic cold open read as genuinely distinct from Part I's town square.
> 2. **Launch Part III** whenever ready — full LAPTOP/BOX handoff block already produced, single message, launch included, per the corrected Law 15. Kling-12, ~$22.68, no override needed.
> 3. **Once Part II is watched, decide kling-40 vs kling-12 as standing for the rest of the series** — Peter's call (`_CANONICAL.md` Law 19).
> 4. **Confirm `finish --assemble-only` completed** on the original river-Enoch (Part I) redo if not already done.
> 5. **+48h FALSIFIER READ** for Part I, timed from actual publish.
> 6. **Cheap ten-minute check:** Worry's motion — kling-coverage hypothesis, not a new debugging thread (`_CANONICAL.md` §7).
> 7. **Once both Parts II and III have read:** author Part IV (Book of Giants) — `chop_river.py` is now the standing tool for this; supply a fresh config, don't reuse Part II's or Part III's token sets (Opening Signature Ledger applies to config content too, not just cold-open prose).
>
> **BRIEFCASE GAPS, fix before next session (Peter's action item, logged so it isn't rediscovered):** `csv2script.py` was missing from the 29 Jul morning briefcase (now confirmed working from a 26 Jul source copy across two sessions same evening — get it into the standing briefcase permanently). `intake.py` has never been present in any briefcase seen by any session this project has record of, despite `_CANONICAL.md` §3 naming it as the tableau-budget enforcer — until it's included, that law runs on manual eyeball only. **NEW this session:** `chop_river.py` (this session's own new tool) should be added to the standing briefcase going forward — it isn't a gap, it's a delivery, but it needs to actually make it into the next packed briefcase to be useful rather than rediscovered from an old zip.
>
> **STANDING LAWS (full receipts in `_CANONICAL.md`):** river-first is now the default authoring sequence, not golden-pair-register-only; never pause uploads; universal beats lore (N=5 operators) — the topic gate on every script; cost = words-per-beat, not minutes; probe before spend; test every differentiator against the channels that FAILED with it; fixing a stills-adjacent bug on an already-rendered project never needs new image spend — the recipe is banked in `_CANONICAL.md` §7. The handoff block is ONE message, all three parts, launch included — no back-and-forth (Law 15, corrected). Peter does not review anything between launch and Studio. **NEW: catch token-overuse and the audit-vs-compile word-count gap proactively, before AUDIT/COMPILE, not after a failure or a manual histogram read — both are now a known pattern, not a surprise, starting with Part IV.**
>
> **BOX/LAPTOP reminders:** edit LAPTOP → git → BOX `git pull --no-edit`; commands from `~/Pipeline` not `~`; never restart `mission-control.service` mid-render; never hand-edit the box. **Claude Code is the named next-horizon tool for closing the command-execution gap** (`_CANONICAL.md` §8) — not yet built.
>
> **OTHER CHANNELS (Synthetic Press, QQrew, Lazarus, Final Hours, etc.) — UNTOUCHED this update.** Everything below this banner outside Sacred Dawn reflects the 25 Jul snapshot; no visibility into what's shipped since. Refresh separately before trusting it.

_The one living operational log. Solo operator (Peter, The Hague). All edits LAPTOP → GitHub → box; never hand-edit on box._
_Last updated: 29 July 2026, late evening (SACRED DAWN — TWO SPOKES IN ONE EVENING: Part II (Astronomy Book) launched at kling-40 and rendering; Part III (Parables of Enoch / Son of Man) authored, audited, compiled, and machine-gated using a newly-built and newly-proven generic tool, `chop_river.py`, ready to launch pending Peter. Angle #1 (Son-of-Man/Gospels parallel), reserved since Part I, finally spent in Part III under full attribution-moat discipline — the real, contested Qumran-absence dating debate stated honestly on both sides rather than oversold. Full narrative: `SESSION-NOTES-2026-07-29-astronomy-book-enoch-part-ii.md` (Part II) and this RECORD entry (Part III, no separate session-notes file written this pass — see the RECORD entry above for the full account). Prior: 28-29 Jul THE RIVER-FIRST PIVOT.)_

---

## How to maintain this doc (read once)

This file replaces the pile of dated `SESSION-NOTES-*.md`. It has two halves:

1. **THE BACKLOG (front)** — everything still open, prioritised. Read at the start of a session, edit at the end.
2. **THE RECORD (back)** — a compressed, reverse-chronological list of what shipped each session. A memory aid, not a working doc.

**The discipline that keeps this from rotting:**
- **Durable lessons graduate OUT of here** into **`docs/_CANONICAL.md`** (the system) or the relevant doctrine doc (`_BRIDGE`/`_LEGO`/channel doc — ante-machinam is retired) — not a worklog entry. Before a block ages out of THE RECORD, lift anything still true-and-reusable into the canonical/ante-machinam first, *then* compress the block to a one-liner.
- **THE RECORD is allowed to be lossy.** Once a lesson is in the canonical, its block can shrink to date + headline. Detail you might want later → a dedicated `SESSION-NOTES-<date>-<topic>.md` in an archive folder, linked from the block.
- **The test for "backlog or done?"** — *does the machine already do this for me?* If yes, move it to THE RECORD.

---
---

# THE BACKLOG

_Standing read: the system is browser-only end to end, uploads across the active channels on non-expiring auth, renders at ~$16.80/video (cinematic) or ~$3/video (Ken-Burns), runs a fully-unattended batch path with a read-side verifier (`dump_channel.py --all --cadence`, §5.10), and — proven 23 June — scales cleanly to feature length (70-min films). The **standing pre-flight before any batch** is the three-gate zero-spend check (parse all scripts → slug-collision scan → `run_all_batches --plan`), and for long-form, confirm `create_project` verify clears (the cheap gates do NOT catch `no_visual`). **The highest-leverage move is shipping real videos and letting first-48h CTR + AVD drive what to fix — not grinding this list.** Operating frame (§2A): automate the labour, concentrate craft on the channel showing a vein, gate on review, cull without sentiment._


## Tier V2 — v2 cutover + v1 decommission (NEW 31 Jul — the standing discipline)

**State: v2 is BUILT and PRODUCTION-PROVEN (Thomas teaser CSV->Studio, 31 Jul). v1 is FEATURE-FROZEN from this date: bugfix-only, via patch_*.py, nothing new ever again.**

The decommission map (banked 30 Jul, now operative):
- **Nine v1 engine files DIE** when their v2 replacement has shipped a real video: csv2script.py, parse_script.py, run_batch.py, orchestrate.py, modea_* legs, draft_moves.py + the batch-inbox format around them. **Two are ORGAN DONORS, already harvested:** recreation_pipeline.py (generate_voiceover / _chunk_text / _synthesize_chunk / generate_still / ken_burns_still / animate_still — all extracted verbatim into v2 31 Jul) and assemble_episode.py (concat / music bed / sidechain mux / kb-tail — extracted 31 Jul). select_thumbnail_still: KILL NOW (errors every run; thumbnails are hand-made).
- **Formats die BY CONSTRUCTION, not by deletion:** the .md pair, beats_full.json, storyboard.json, durations.json, render_policy.json, batch inboxes, the exists-guard/-src dance — none has a v2 consumer. The teaser proved a project with none of them.
- **ATTIC RULE (three conditions, all required, per file):** (1) replacement has shipped a real video, (2) zero grep references from live code, (3) no still-active channel needs it. Until all three: the file stays where it is, frozen. Never delete on the box without the laptop-side grep first.
- **MIGRATION ORDER:** Sacred Dawn pilots v2 (DONE — teaser). Next NEW Sacred Dawn spoke (Part VI Thomas) renders on v2. Scripture On Screen second. Synthetic Press third. Parked channels migrate only on reactivation — a parked channel never justifies keeping v1 alive.
- **HARD RULES:** shared/v2/ imports NOTHING from v1, ever. No mode flags anywhere — a channel is on v1 or v2 purely by which command runs. Every schema change = numbered migration; code refuses unknown schema versions (db.py enforces).
- **Mission Control:** replaced by `render.py --status` for v2 projects; MC stays alive only while v1 renders exist. The never-restart-mid-render rule applies to MC only; v2 has no daemon to kill.

## Live state / pending publish (operational, not backlog)
- **★★★ SYNTHETIC PRESS (channel `synthetic`) — SHIPPED 3–4 Jul, the blockbuster-register launch. The day the doctrine retrofit got proven.** Blockbuster AI reconstruction of history's catastrophes; Bay/Woo register (teal-orange, bright, faces-forward); ElevenLabs Brian @1.10. Full doctrine `_Synthetic2.md` v1.2; day-narrative in `SESSION-NOTES-2026-07-03-gettysburg-launch.md`.
  - **GETTYSBURG trailer** — 45s, 16 beats, LIVE 7pm NY on the 163rd anniversary of Pickett's Charge. AVD 94.8%; poster thumbnail ("AMERICA'S / DEADLIEST DAYS" then the film's solo GETTYSBURG poster); pinned anniversary comment. Small-n CTR artifacts (133%, 50%) banked as the "coin still in the air" examples.
  - **GETTYSBURG – The Movie (2026)** — 185 beats, **7:03**, LIVE 9am NY 4 Jul. All-trim by construction (zero beats over the 5.04s Kling atom); Filmora music pass; real chapters from `durations.json` (0:00/0:44/1:47/3:22/4:57/5:13/6:10); solo-noun poster thumbnail (starts the HIROSHIMA→EASTLAND series signature). First-day reads (small-n, shape only): **20% CTR / search-led ~53% / Browse OFF ZERO ~6%** — the cold-start needle twitching. Funnel wired both directions (trailer↔film end-screen/card/links).
  - **~30 vertical SHORTS** cut from the film on first-spoken-word impact, **title = the opening line verbatim**, square-centre-blur format, scheduled hourly through evening 5 Jul. First run of the Shorts leg (`_Synthetic2.md §14`). **The read is the distribution of all 30, not any single Short.**
  - **OPEN:** thumbnail `candidate_prompt_suffix` still FH-mournful (rewrite before auto-thumbnail use); ~~the 2 cost-control MC toggles + MC thumbnail-upload/scroll-to-top~~ **ALL SHIPPED 04 Jul evening (MC v3.6 — see RECORD)**; chapter head-offset spot-check (Filmora added ~10s).
- **★★ @Q-Qrew (CHANNEL #12) — the anti-Final-Hours bet. Ep1+Ep2 SHIPPED; Ep3 BLOCKED on the prompt-construction fix.** Bright, fast-cut, character-driven, flat-cel, true-static curiosity-explainer — deliberately the opposite of Final Hours; the recurring CREW (Driver/Skeptic/Brain) is the durable IP. Full doctrine in `_QQrew.md`; narrative in `qqrew/docs/SESSION_NOTES_QQrew_FULL_DAY.md` + `MASTER_SESSION_NOTES_crew.md`.
  - **Ep1 (soap, "Humans Went 200,000 Years Without Soap")** — SHIPPED 29 Jun, Driver-solo, 238 beats, true-static 7:22, Evan@1.05. Peter's verdict on watch: "very very very very good." The pilot that proved the channel renders + the character holds.
  - **Ep2 (ice age, "The Disturbing Things Humans Did to Survive the Ice Age")** — SHIPPED 30 Jun, 207 beats static, category 27; Skeptic debuted in 13 reaction cutaways.
  - **Ep3 (pregnancy, "Being Born Almost Killed You…")** — Skeptic's first SOLO-LEAD, 213 beats, Zadie@0.95, crunchy verified data. **RENDER UNBLOCKED 30 Jun** (folder rename + canon merge — see THE RECORD) → 213 stills rendered clean. **THEN BLOCKED at the stills gate: the stills came back photoreal beauty-portraits, not flat-cel — caught before any animation spend.** Two prompt-construction faults (style_suffix not appended; 120-word canon eats the prompt). **DO NOT SHIP until the §11 P0 fix lands + a 6-beat probe re-render clears the bar.** Voiceover/durations/canon all banked at `qqrew/projects/pregnancy1/`; only the prompt fix + re-render remains.
  - **Channel config:** folder now `qqrew/` (renamed from `crew-wip/`), `base_canon` carries Driver+Skeptic, voice flips per-episode (Option A; Option B per-character voice required at Ep4/Brain). OPEN: `category_id` still "24", fix to "27"; `crew-wip`→`qqrew` script source-home migration (P9).
- **★★ LAZARUS FILMS — Yellow Wallpaper LAUNCH (28 June) — rendered, music'd, READY TO HAND-UPLOAD.** First Lazarus film: `wallpapervfinal` project (NB the live cut is in `lazarus/projects/wallpapervfinal/`, not `yellow-wallpaper`/`yellow-wallpaper2` — those are stale daytime renders). 25 beats, 7:33 (453.4s, voice-pinned), Rosalind/0.85, modern-exposed look, all stills reviewed frame-by-frame through Mission Control. Final assembled FROM THE BOX with music (`assemble_episode.py --project lazarus/projects/wallpapervfinal --durations …/durations.json --index …/_index.json --voiceover …/voiceover.mp3 --clips …/clips --music-dir lazarus/music`) because Mission Control drops music (#30). Thumbnail DONE (banger — freckled/blotchy/exhausted anti-stock anguish face, "IT MOVES / AT NIGHT", amber subtitle; the blue box was a preview-window artifact, not in the file). **ACTIONS before publish:** download final from box; hand-upload via Studio (OAuth unwired, #32); set THIS thumbnail manually (don't let Studio pick a frame grab); title/description (in `_Lazarus.md` / session); category = Entertainment; tags from header; **Altered content = Yes**. Astana AI Film Festival entry (deadline 15 Aug 2026, ≤10 min, free) once it proves out.
  - **The candlelit-render rejection + bias rebuild (the session's spine, → §6 of `_Lazarus.md`, already banked there).** First render (Tell-Tale Heart) came back period-gothic/candlelit → REJECTED ("I personally wouldn't watch this"). Root cause: model AND assistant default toward darkness/period-gothic (median cliché; darkness hides model flaws). Correction = **McEwan thesis: dread in the bright, fully-lit, present-day ordinary.** Proven across ~6 cheap stills tests, operator's eye as gate. Six channels staged via `stage_batch.py` and run through `run_all_batches.py`, each filling from day-after-its-own-tail to a common **09 July** finish (Soak 12 from 28 Jun, Dawn 9 from 01 Jul, Prehistoric 7 from 03 Jul, YHTBT 2 from 08 Jul kling-0, Cathedral 1 on 09 Jul, Scripture `jericho` 1 filling the 04 Jul gap). **ZERO transient-API blips at 32× exposure — the #11 retry gap didn't bite this time.** All thumbnails confirmed rendered + good in Studio. Inboxes swept post-run (32 pairs → `_shipped/`, the third manual sweep this arc — see #11n). **The portfolio is now LEVELLED: all six daily channels continuous through 09 July, the Scripture 04 Jul hole filled → the NEXT batch gets a single shared `publish_start` of 10 July.** Channel-doc shipped-ledgers (pushed this session) are exactly accurate — clean run, no "pending resume" caveats. **Pending: a `dump_channel.py --all --cadence` once uploads propagate in Studio to formally confirm zero gaps.**
- **★★ The two FEATURE FILMS — PUBLISHED Sat 27 June 01:00 CEST.** `watchers-the-movie` (sacred-dawn, 360 beats, **74.8 min**) + `revelation-the-movie` (scripture-on-screen, 355 beats, **69.7 min**), both `--kling-count 40` (~$17 fal each). Both confirmed live, public, thumbnails attached. The pipeline's first feature-length renders — production-system spans 8-min shorts to 70-min features with no break.
  - **★ EARLY ANALYTICS FINDING (27 June, ~9h post-publish) — the format-vs-channel correction (IMPORTANT, → graduate to doctrine).** First read at 8h looked grim (Watchers 18 impressions, Revelation 50, ~0 CTR) and almost produced a WRONG "feature-length doesn't distribute" conclusion. Two things corrected it: (1) **the impression curve is a delayed hockey-stick** — flat 0–8h then sharp acceleration after hour 8 (Revelation 50→148 impressions in the next ~90 min). **Long-form has a distribution RAMP a short doesn't; an 8-hour read on a 70-min asset is unreliable and nearly killed the format prematurely.** (2) **Measured against its OWN channel, Revelation is the #1 recent video by views** (5 views in 9h39m vs the channel's normal-length episodes at 3/3/1/0/0/0…). **So the feature is NOT starved relative to the channel — it's the top performer. The real constraint is CHANNEL-LEVEL distribution** (Scripture On Screen isn't getting impressions for ANY format yet — a cold-start-channel problem, not a feature problem). **The open question is now narrowed to long-form AVD: Revelation early AVD 3:18 on a 69-min film (~5%) — too small at n=5 to trust, but THE number to watch as views accumulate.** If a committed-watcher tail emerges, feature-length is validated; if AVD stays single-digit-minutes, 70-min is too long for this audience. **ACTION: read both features properly at ~48h (29 June) via NexLev — AVD + the impression ramp, not the 8h noise.**
  - **Thumbnail A/B tests set up on BOTH features (27 June).** Revelation: original beast-still vs a new motion-frame still, both "THE FOUR HORSEMEN". Watchers: original vs `shot_119` motion-still, both "THE WATCHERS". Built via `make_thumbnail.py --still <frame>` (composites the current wording onto an extracted frame, reusing the channel `figure_right` block — same placement as live). Running in YouTube Test & Compare (optimizes WATCH-TIME not clicks, §canonical). **Caveat: both A/Bs are starved by the same low impression volume — won't resolve for weeks; the A/B optimizes clicks on impressions we're still growing, it does NOT fix the channel-distribution constraint.** NB the swap means any future Revelation/Watchers CTR is measured across two thumbnails — note the A/B start when reading.
- **★ The 55-video batch-of-batches (23 June) — 54/55 shipped clean.** prehistoric (10), sacred-soak (5), you-had-to-be-there (10, kling 0), cathedral_of_stars (10), scripture-on-screen (10), final-hours (10). One straggler: **`jacob-esau` (scripture) FAILED at the stills leg** (vo + publish.json present, no final_video, 0 stills — transient fal blip, isolated cleanly). **RESOLVED 26 June: `jacob-esau` ABANDONED — the fresh `jericho` video (levelling batch) fills the 04 July slot it would have taken. The half-built project is NOT being resumed; #17 Jacob & Esau stays in the Scripture backlog as an unbuilt topic if wanted fresh later.** **Inbox sweep DONE (26 June): all seven channels' 55-run + feature pairs swept to `_shipped/` (47 pairs) before the levelling stage — inboxes were clean-verified empty before staging.**
- **★ Satan flagship (Sacred Dawn, `satan-morning-star`, 89 beats, ~26.4 min, 45 Kling) — SCHEDULED 21 June 20:00 CEST.** Worst video rebuilt into strongest film. **ACTIONS:** confirm bottom-left thumbnail + chapters (0.040 re-mux); Altered/AI flag; move pair to `_shipped/`; **DIARISED ~mid-July: pull Satan retention vs the original 15.7%.**
- **gustloff** (Final Hours) — private, `wiykuEhTY1k`. **ACTION: set Altered = Yes before publishing.**
- **★ Lady Be Good bomber (Final Hours, `PCTEasHZjwI`, 6:45) — ORPHAN from 19 June.** Never reviewed, rescheduled 29 June 04:30 CEST. **ACTION before 29 June: open, set Altered/AI = Yes, eyeball, re-time to 01:00 CEST if wanted.**
- **Standing practice (21 June):** after every batch, `dump_channel.py --all --cadence` in Amsterdam time — fill any GAP, scan orphans/collisions.
- **Esther** (Scripture, 119 beats) / **Enoch** (Sacred Dawn, 132 beats, pre-pivot, NOT the 29 Jul river-authored Part I) — rendered; need packaging/publish. Status unconfirmed since 25 Jul.
- **70smusic** (You Had To Be There, 105 beats) — confirm done + publishable.

## Tier 1 — Ship & verify (the real work)
0. **★★ SYNTHETIC 48h READ (evening 5 Jul) — (b) the two MC cost-controls ✅ SHIPPED 04 Jul evening, MC v3.6 (see RECORD; only the inherit ARTIFACT proof remains).** (a) **48h read evening 5 Jul** — Gettysburg film CTR/AVD/**Browse%** + long-form retention curve (first all-trim AVD data at 7 min; gates Elijah length) + **Shorts distribution shape** (30 = one experiment). Two signals before doctrine moves. (b) **Build the two per-beat cost-controls on the stills-review page** (`_Synthetic2.md §7`): cost is a Kling-count problem (~92% animation, ~2.2× all-trim footage overhead); recover it WITHOUT touching sync (beat timeline immutable, only pixel-source changes). **Read the assembler's beat→shot→audio binding FIRST** (never guess) → `patch_kenburns_toggle.py` (static beat → free programmatic push; also safer — no invented motion artifacts; isolated, ship first) → `patch_clip_merge.py` ("previous clip covers this" — merge two short same-scene beats into one 5s clip, render nothing for the second, recover the trimmed footage, eliminate that beat's re-render tax). Safe only on the motion-pinning clusters; the gate is narrative-function (meaning in the camera-move vs the stillness), an editor's-eye call. **This makes the review page the surface where craft-judgment and cash meet per beat — the core craft-vs-automate lever.**
0h. **✅ DONE 01 Jul 2026 — was a MISDIAGNOSIS. Real fix: channel `style_suffix` content (flat-cel webcomic → semi-realistic bright; `_QQrew.md §4/§6a`). Canon-tag cut shipped too, minor. Mercator Ep3 rendered trio-tier. Full lesson graduated to canonical (style_suffix = highest-leverage look lever; read it FIRST).** ~~★★ QQrew P0 — THE PROMPT-CONSTRUCTION FIX (ship-blocks Ep3; channel-quality-critical; banked 30 June).** Ep3's 213 stills rendered photoreal beauty-portraits, not flat-cel. Two faults (full spec + commands in `_QQrew.md §6a` doctrine + `§11 P0`): (1) the channel `style_suffix` is NOT being appended to QQrew's prompts (the "NOT photorealistic" lever is absent → flux defaults photoreal); (2) the ~120-word beauty-portrait canon leads every prompt and drowns the scene (flux weights the front). **Fix:** append the real `style_suffix` in the prompt-assembly step; rewrite the crew canon in `base_canon` to a ~20-word identity tag (strip the photoreal-bait words); order the prompt scene-first / canon-trailing. **Then a 6-beat probe (~$0.18) before the full 213 re-render.** Free diagnostic first: check whether Ep1(soap)/Ep2(iceage) storyboards HAD the suffix and how long Driver's canon was — confirms regression vs latent fault. **This is the gate between "the machine renders" (proven) and "the channel looks right" (not yet).**
1. **Read first-48h CTR + AVD as data matures** — prehistoric ep1/ep2, the 55-run wave, the levelling-batch wave, and the big one: **the two features. ★ DIARISED ~29 June (48h): read Watchers + Revelation AVD via NexLev — does long-form retain? Revelation early AVD 3:18 on 69 min (n=5, untrustworthy). This is THE feature-length verdict — not the impression count (channel-distribution-limited), not CTR (A/B-blended), but whether the committed-watcher tail materialises.** Cold-open first-90s remains the game on long-form. **(QQrew Ep1/Ep2 48h CTR+AVD also now due — crank-or-park read.)**
2. **Close the gaps the runs surfaced** — manifest ✗ check, Studio spot-check (private + 01:00 `+02:00` + thumbnail), sweep inboxes before re-run. **Gaps (Tier 2):** (a) re-ingest auto-archive (#11n); (b) thumbnail-set retry + persist video ID + `set_thumbnail.py`; (c) `--plan` → `validate_slug` + `thumb.json` schema (#11m); (d) `--plan` → `create_project` verify so `no_visual` fails cheap (#11o).
3. **~~Resume `jacob-esau`~~ — DONE (abandoned 26 June; `jericho` fills the slot).**
4. **Prove dramatic motion on a Kling-heavy render** — `cain-abel`. (The features' 40-Kling front-loads are now real evidence the tiered path holds at scale.)
5. **Get pending-publish videos out** (gustloff, Esther/Enoch).

## Tier 2 — Correctness / safety still genuinely open
6. **Retire or guard `finish --assemble-only`** — calls alignment-UNSAFE `recreation_pipeline.assemble()` (drifts). Still CLI-reachable.
7. **Confirm `cmd_stills` passes `safety_tolerance:"5"` on the FIRST stills pass.** Confirm in code, close.
8. **`finish --plan` not side-effect-free** — mkdirs before early-exit + CWD-relative path. Tiny.
9. **`proj_paths` bug — WORSE than documented.** `assemble_episode.py` falls back to hardcoded `projects/ep1-the-promise/` for file lookups AND `mkdtemp` work-dir when `--project` doesn't resolve. **Fix:** `--project` drives ALL derived paths, kill the literal, fail loudly. **Workaround (§13): re-mux needs `--project` AND explicit `--durations/--index/--voiceover/--clips`, OR run from inside the project dir.**
10. **Inworld model-string reconcile** — doc/code drift; live model `inworld-tts-2`, `speakingRate` in `audioConfig` works.
11. **Inworld chunk-validation guard — GENERALISE to all-API retry-with-backoff.** A failed/empty chunk should retry or hard-fail, not silently concatenate dead air. **Re-confirmed TWICE: `lost-human-species` (Inworld, audio) and `jacob-esau` (fal, stills) were both single transient-API blips with no retry. The durable fix is GENERIC retry-with-backoff around EVERY external API call (audio + stills + anim), not just Inworld — at feature length (~360 chunks) the exposure is ~9× a short. Highest-leverage reliability fix on the board.**
11p. **Dead OAuth token CRASHES headless `dump_channel.py --all` instead of skipping (MED — NEW, 26 June).** Success Coach's `peteralkema6@` token is dead (`invalid_grant`) and took down the *entire* headless `--all` dump by trying to launch a browser consent flow on the box. **The canonical claims a skip-guard exists; the traceback proves it doesn't.** Interim workaround applied: `EXCLUDE = {"success-coach"}` in `dump_channel.py` (committed) — keeps it visible-but-out-of-rotation. **Real fix: wrap each channel's token load in try/except; a dead/expired token logs a warning and SKIPS that channel, never launches an interactive flow in a headless run.** Re-mint Success Coach separately (needs `peteralkema6@` login + its own `client_secret.json`) to restore it to the sweep.
30. **Mission Control assemble/re-assemble SILENTLY DROPS MUSIC (HIGH — NEW, 28 June).** Neither the panel's auto-assemble nor the "Re-assemble (latest clips)" button passes `--music-dir` to `assemble_episode.py`, so every Mission-Control-built final is voiceover-only — no bed, no warning. The beds exist (`lazarus/music/`, 7 tracks) and the assemble code handles music perfectly; the panel just never wires the flag. **Interim: finalize every Lazarus video FROM THE BOX** with the explicit `assemble_episode.py … --music-dir lazarus/music` command (proven 28 June — re-muxed Yellow Wallpaper's existing clips with a crossfaded bed at 0.040, no re-render). **Two real fixes: (a) patch Mission Control's assemble handler to always pass the channel's `--music-dir` (needs the MC server file — `systemctl --user cat mission-control.service | grep -i exec` then read its assemble call); (b) belt-and-braces — make `assemble_episode.py` FAIL LOUD when no bed is found and `--no-music` wasn't explicitly passed, so a music-less final can never silently ship from ANY path. (b) is the one that makes the bug class impossible.** This is the music-ghost bug's third confirmed head (batch path defaults to generated music; re-assemble drops it entirely).
32. **✅ DONE 04 Jul evening (MC v2.9→v3.0, superseded by the source-mode design: upload = persistent `thumbnail_source.png`, Generate branches `--shot`/`--still`).** ~~MC thumbnail image-UPLOAD path (LOW — NEW, 4 Jul).~~ The MC thumbnail panel only composites from a rendered still; add a file-input so an externally edited image can be set as the project `thumbnail.png` (small endpoint: accept upload → save to project dir → refresh preview). Real need: iterating the poster in an editor and pushing it back.
33. **✅ DONE 04 Jul evening (MC v2.8).** ~~MC scroll-to-top button (LOW — NEW, 4 Jul).~~ Fixed-position button at the bottom of the storyboard; a 185-beat grid makes the scroll-back a hand-cramp. Trivial template patch.
34. **MC thumbnail block per-channel default (LOW — NEW, 4 Jul).** A new channel with no `thumbnail` block renders on raw defaults (title auto-sizes to fill, wrong subtitle colour). Synthetic inherited the FH cinematic block manually; the panel could fall back to a sane cinematic default + warn, so a new channel's first thumbnail isn't broken. (Subtitle colour is an RGB list `[R,G,B]`, not hex — bit us; the candidate_prompt_suffix also needs a synthetic-native rewrite before auto-thumbnail use.)
31. **Slow-fill clip-stretch 3–4.7× (MED — NEW, 28 June).** On the Yellow Wallpaper assemble every beat logged `slow-fill … (heavy stretch; candidate for more/shorter beats)` — clips are ~5s but beats run 14–23s of narration, so the assembler stretches a 5s clip to fill a 20s beat. Works for the contemplative near-frozen Lazarus register (the dread WANTS held motion), but it's not Kling motion across the whole beat — it's a short clip slowed. **Fix later: longer clips, or more beats with shorter narration each, so clip-length ≈ beat-length. Tuning note, not a launch blocker — on a slow-burn film it reads as intended.**
32. **Lazarus OAuth unwired — hand-upload (MED — NEW, 28 June).** Lazarus has no valid token and no `client_secret.json` in the channel folder (`upload_episode` fails: `no valid token and no …/lazarus/client_secret.json`). Harmless for a hand-launched film (manual upload is wanted anyway, to set thumbnail/end-screens/Altered-content). **Real fix (multi-part, non-launch-night): drop `client_secret.json` into `lazarus/`, run the OAuth flow once on the LAPTOP (browser can complete it), rsync the minted token to the box, handle the test-mode 7-day token expiry.** Same shape as the original Final-Hours OAuth task.
33. **CODE-LEAK: `_engine_project` basename-collapse (HIGH — NEW, 30 June; canonical §2B item 4).** `modea_leg._engine_project` resolves a project by `basename`-from-`run_cwd`, silently assuming the portfolio invariant `<channel>/projects/<slug>`. For QQrew's `crew-wip/projects/` (one level deeper) it invented `./<slug>/` and the engine loaded the wrong empty folder → stills died before the first PNG, with the error swallowed by MC. **Worked around by renaming `crew-wip/`→`qqrew/` (honour the invariant).** Real fix: root the engine at the *real* project dir, fail loud if it doesn't exist; never basename-collapse.
34. **CODE-LEAK: canon precedence — engine loads `base_canon`, ignores project `canon.json` (HIGH — NEW, 30 June; canonical §2B item 3).** QQrew Ep3 died with `Unknown canon tag(s): ['skeptic']` because the Skeptic canon sat only in the project `canon.json` and the engine's stills path read only `base_canon`. **Worked around by merging the whole crew into `base_canon`** (correct home for recurring cast anyway). Real fix: the engine must MERGE `base_canon` + project `canon.json`, project-wins-on-conflict — so per-episode guest/override canon works on a character channel.
35. **DEFERRED: the ante-machinam footer scrub (LOW — NEW, 30 June).** `ante-machinam.md` is RETIRED + tombstoned (redirects to its new homes), so this is safe to defer — but ~13 LIVE docs still carry stale "craft in ante-machinam" footers/refs that should be repointed: `_Synthetic.md`, `_Sacred-Dawn.md`, `sacred-dawn-creed.md`, `_Scripture-On-Screen.md`, `_Prehistoric-Disasters.md`, `_you-had-to-be-there.md`, `_Woodworking.md`, `_Lazarus.md`, `README.md`, `machina.md`, `__ORCHESTRATOR-DEPENDENCY-MAP.md`, `final-hours/docs/the-forgotten-sea-series-bible.md`, and this worklog's own "→ ante-machinam §6" graduation notes (#27/#28/#29/#29b). PLUS neutralise `patch_ante_machinam_2026-06-17.py` (it edits the now-dead file). Leave all `archive/`, `SESSION-NOTES-*`, and `patch_*` snapshots ALONE — historical record. One clean grep-driven commit, own session.
36. **QQrew channel build-queue (→ `_QQrew.md §11`):** P0 prompt-construction (above, Tier 1); P1 audio-seesaw continuous-voice (= portfolio #11-class, the same continuous-synthesis fix); P2 true-static native in `_still_to_held_clip` (= canonical §2B item: kling_count:0 still applies Ken-Burns); P3 canon+render_policy auto-write at create (= the MC-path canon divergence, #34-adjacent); P4 vision-judge JSONDecodeError (portfolio-wide silent candidate-1 fallback — best-of-N has never actually run on the box); P5 channel-agnostic upload step + batch exit-gate (category 27 for QQrew); P6 `--unattended` still hits the stills gate; P9 `crew-wip`→`qqrew` script source-home migration + category_id 24→27 fix.

## Tier 2.5 — Engine upgrades (Satan flagship session, 20 June)
11b. **Thumbnail `--text-anchor` flag (HIGH).** Add the override + a `text_corner` return from the substrate-selection Sonnet call. Bottom-left = reserved flagship signal. → `_Sacred-Dawn.md` §7.
11c. **Music `level`-key wiring + LUFS-target normalisation (HIGH).** Wire the inert `level` key; replace fixed `MUSIC_LEVEL` with a measured-LUFS target. 0.040 flat is interim. → §5b. **Feature-length note: the two 70-min features used the fixed 0.040 bed — spot-check it holds across 70 min.**
11d. **Chapters/description auto-builder.** `audio_start` → `mm:ss` chapters + template. → §7b. **A 70-min feature NEEDS chapters more than any short — high-value for the two features.**
11e. **Score-aware music placement (horizon).** Whisper timestamps + per-beat energy → intentional scoring.
11f. **rsync over scp for box pulls (§13).** Re-confirmed: inboxes gitignored — pairs by scp/rsync, not git.

## Tier 2.6 — Read-side / cadence upgrades (21 June)
11g. **Cadence: read `batch_inbox/` for TRUE runway** (today only sees Studio-scheduled).
11h. **Runway alarm** — flag any channel whose span ends within N days.
11i. **Golden-hour check** — flag any slot not at 01:00 CEST.
11j. **Auto-run cadence at the tail of `run_all_batches.py`.**
11k. **Read-views over `channel_dump.json`** — `artlist_worklist` + `missing_chapters`.

## Tier 2.7 — Phone-first logging rewrite (23 June)
11l. **Rewrite the orchestrator's visible logging — phone-first, surgical (HIGH).** The current stream is human-gate-era — the **ASCII-art banner** ("Peter's Pipeline Orchestrator") + `phaseStrip` + program-narrating lines. **Scrap all of it, ASCII art included.** Peter monitors multi-hour runs from his phone (Termius); each poll line must answer "where, how far, healthy, do I act" from the LAST VISIBLE LINE ALONE.
   **Principles:** self-contained lines with `[chan N/M · vid N/M slug]` coordinates; fractional progress on looping phases (`voiceover 7/12`, `flux 41/79`, `encode 9m/≈18m`); fixed vocab `AUDIO · STILLS · ANIM · ASSY · THUMB · UPLOAD`; heartbeat-age tail (`·15s ·30s`) as stall signal; emit-on-change; surgical failure lines; channel roll-ups. Keep 15s cadence.
   **Target stream:**
   ```
   [2/6 prehistoric · 6/10 messinian]  ▶ START  79 beats · kling 2 · ~18min est
   [2/6 prehistoric · 6/10 messinian]  AUDIO   voiceover 7/12 chunks                 ·30s
   [2/6 prehistoric · 6/10 messinian]  AUDIO   ✓ done · 18m32s locked · 79 beats timed
   [2/6 prehistoric · 6/10 messinian]  STILLS  flux 41/79 · safety_tol 5             ·45s
   [2/6 prehistoric · 6/10 messinian]  ANIM    kling 2/2 ✓ · ken-burns floor 77/77
   [2/6 prehistoric · 6/10 messinian]  ASSY    ffmpeg encode 9m/≈18m · preset medium ·30s
   [2/6 prehistoric · 6/10 messinian]  THUMB   ✓ cand 2 (clean left col) · headline baked
   [2/6 prehistoric · 6/10 messinian]  UPLOAD  → private + publishAt 06-27 23:00Z
   [2/6 prehistoric · 6/10 messinian]  ✓ DONE  18m32s · publishAt Sat 06-27 01:00 CEST
   ```
   **Where it lives — NOT one file (the key insight).** The line needs three contexts from three layers: batch position (WRAPPERS `run_all_batches.py`/`run_batch.py`), phase (ORCHESTRATOR `orchestrate.py` + legs), loop fraction (ENGINE `recreation_pipeline.py` — the only layer inside the `for chunk/still/clip` loops). No single file has all three — which is why the current logging is dumb. **Fix: a thin shared emitter (`pipeline_log.py`) + a context object threaded wrappers → orchestrator → legs → engine.** Call sites go from `print("generating voiceover")` to `log(phase="AUDIO", n=7, total=12)`.
   **Build step 1 (read-only spec):** grep every `print(`/log call across the four layers; map each to its phase; that trace IS the spec.
   **Known unknown:** the encode fraction needs ffmpeg's `-progress` pipe, which `assemble_episode.py` may not wire — the one phase that may need plumbing. (Earns its keep most on the 70-min features.)
   *One focused build on a non-launch day — touches four layers; don't nibble.*

## Additional gaps surfaced 23 June
11m. **`--plan` does NOT validate `thumb.json` schema — CLOSED by `stage_batch.py` (26 June).** Cathedral's 10 thumbs were present + paired but missing `subject` → all 10 `prep_failed` at run-time. The fix shipped as a **pre-stage gate**: `stage_batch.py` asserts `subject` + `title` non-empty before a pair ever reaches the inbox (proven 26 June — it rejected Cathedral's `21-milky-way` for a missing `subject`, fixed at source, re-staged clean). `--plan` itself still doesn't check this, but nothing reaches `--plan` without passing through `stage_batch` first. *(Could still add the assert to `--plan` as defence-in-depth — LOW now.)*
11n. **✅ DONE (applied + proven 25 Jul — chambers pair auto-archived to `_shipped/` on ok=True).** ~~Auto-archive shipped pairs STILL open (MED — `patch_batch_archive_shipped.py` drafted 19 June, never applied).** Runs leave pairs in `batch_inbox/`; the collision guard is the real protection, but inboxes must be hand-swept to `_shipped/`. **Re-confirmed 26 June: all seven channels still held their 55-run + feature pairs; swept 47 pairs by hand before the levelling stage.** Apply on a non-launch day: archive on `ok=True` only.
11o. **`--plan` does NOT run `create_project` verify, so `no_visual` slips to spend-time — CLOSED by `stage_batch.py` (26 June).** `watchers-the-movie` `prep_failed`: 22 of 360 beats had narration but no `VISUAL:` line. The fix shipped as a **pre-stage gate**: `stage_batch.py` calls the REAL `ingest.verify_beats` on every pair (proven 26 June — caught a planted `no_visual` beat in testing), so `no_visual`/`wordless` are rejected before staging, at zero spend. **Open design question still live: were the 22 MISSED or authored to hold the prior image?** If the latter recurs, the better fix is a pipeline "inherit previous VISUAL when none given" rule — `stage_batch` would then need its verify relaxed to match.

## Tier 3 — Console / cosmetic polish
12. **Mission Control thumbnail integration** — capture thumbnail text in create flow; show generated thumbnail beside final video with regenerate/accept.
13. **Tiered-aware strip label** — *subsumed by #11l*.
14. **Kling-count field into the always-visible controls strip.**
15. **`voice_id` cosmetic label** (`patch_voice_label.py` drafted).
16. **Gate-button human/wire split remnants.**
17. **Upload polish** — per-channel `upload` blocks; rename OAuth consent app → "Pipeline."

## Tier 4 — Bigger builds
18. **Upload scheduler — SHIPPED 19 June, proven at scale (55 videos) and feature length (2 films).** `--publish-start` + `--publish-interval-hours`; private + publishAt; `--plan` calendar. Day-after-furthest-tail = no-state collision rule; for features hand-set a Friday-US-prime slot (an event, not daily cadence). → §5.9. *(Done; design-of-record.)*
19. **Faster final encode — PROMOTED by feature length.** `-preset medium -crf 18` is the heaviest step (~20 min for a 20-min video; HOURS for 70-min). `-preset fast`/`veryfast` on Ken-Burns lanes ≈ halves it, no visible loss. Biggest throughput lever now 70-min encodes are in the mix. Verify on one video first.
20. **Decade-look Phase 2** — `film_emulate.py` does not exist.
21. **v2 motion routing** — route Kling by which beats EARN motion, not positional first-N. *(Features used positional front-40, authored front-loaded so it held — the latent limit.)*
22. **Parallel fal animation** — bounded-concurrency semaphore; matters on 40-Kling features.
23. **Batch orchestration polish** — re-run story; optional parallelism (sequential now).
24–26. **Synthetic Mode B** — within-card word-sync; design-on-page; Remotion component gaps.

## Tier 5 — Authoring discipline (craft doc, not code)
27. **Beat-granularity calibration** — graduated to ante-machinam §6 (~14s/beat).
28. **Script-format-from-exemplar** — graduated to ante-machinam Part VI.
29. **Slug rule** — `^[a-z0-9][a-z0-9-]{0,60}$`: lowercase, digits, hyphens; NO underscores, NO `NN_`. Numeric prefixes fine with a hyphen (`11-`…`20-`). Bit Cathedral twice. → §12 + ante-machinam.
29b. **Runtime estimator is WRONG for Inworld voices — the feature-length calibration (NEW → ante-machinam §6).** The `max(beats×14s, words/120wpm)` estimate overshot the two features by ~30 min. MEASURED: **watchers 12,391 words / 74.8 min = 165.6 wpm; revelation 12,421 words / 69.7 min = 178.2 wpm** — far above the 120wpm floor (consistent with Inworld ~190–200 raw, slowed to ~165–178 for reverent channels). The floor compounds to ~30-min error at ~12k words. **Author features TO a target at ~165–178 wpm (a 90-min feature ≈ 15,000 words, not 11,000).** The spread (Ren ~178 > Elliot ~166) is per-voice pace. NB ESTIMATE-only: runtime is set by actual narration, so a "short" feature is COMPLETE, just faster — verify via video-duration == voiceover-duration, NOT against the estimate.

## On the shelf — deliberate next analysis (do not lose)
- **Thumbnail packaging audit — the predicted-CTR experiment (TWO stages).** **Stage 1:** sweep every uploaded title+thumbnail pair, score each as the combined unit via Sonnet vision on a PER-CHANNEL rubric, emit a **committed predicted-CTR / predictor score + sub-scores (legibility, subject-text separation, complement-not-echo, register fit, scale-anchor, title curiosity-gap) + rationale** per video. **Freeze it — dated, committed to `shared/audits/thumbnail-audit-<date>.json`, NEVER re-touched once views land.** The frozen-before-outcome prediction is the entire methodology: *it is too easy, consciously or not, to "remember" a thumbnail scored well once you see it performed well.* Record the correlation hypothesis up front (open: relative-rank-within-channel vs absolute-CTR — lean relative-rank). **Stage 2 (weeks later):** pull actual first-48h CTR via NexLev, join by slug, test predicted-rank vs actual-rank AND per-sub-score correlation. Strong → validated instrument, becomes an authoring gate. Weak → our eye and the audience diverge. Unit = **thumbnail + title together**; CTR is the matched outcome; AVD kept separate.

## Banked-for-later / low
- **`.gitignore` for generated artifacts** — `render_policy.json`, `thumbnail_selection.json`, `*.pre_*`, `_index.json`, `channel_dump.json`, `publish.json` (outputs not source).
- **Cleanup pass (23 June):** stray `~/batch_inbox`, `~/batch_rerun_3`, `prehistoric-disasters/inbox/`, `_hold_revelation/` (revelation now rendered — the hold dir can go). Canonical inbox is `<channel>/batch_inbox/` only.
- **review-server / multi-project daemonized review server** — superseded by Mission Control.

---
---

# THE RECORD (compressed, newest first)

### 31 July 2026 — V2 BUILT + PRODUCTION-PROVEN IN ONE SESSION. PARTS IV+V SHIPPED. TEASER = FIRST V2 VIDEO IN STUDIO. THE PIVOT SESSION.

The largest single-session advance in the operation's history, in four movements:

**1. Parts IV + V authored, gated, launched (the v1 spine's closing argument).** Part IV (Book of Giants, 289 beats, 11,519 words, ~70 min, kling-12, $29.16) and Part V (Book of Jubilees, 291 beats, 11,509 words, ~70 min, kling-12, $29.32) both authored river-first, gated green (archetype law native from birth: IV top archetype 20.1%, V 14.9% -- vs Part III's retro-tagged 26.1% seated_authority; entropy 0.891 -> 0.939 -> 0.964 monotonic; seated_authority + reader_by_fire held at literal ZERO in both by construction), launched sequentially in one tmux chain. Session economics banked: two gate-green 70-min films in one day, $58.48 combined. **Giants STUDIO VERDICT (31 Jul): loved it -- the dream-frame cold open banked as a standing mechanism (Law 24, DREAM LICENSE): declaring "this is a dream" in the opening pre-forgives render hallucination and apocryphal weirdness; the weirder the spectacle the better (giant's-eye close-ups singled out). The audience wants spectacle, not dusty books on lecterns -- the Inception effect.**

**2. Jubilees launch failed at the BOX verify gate and taught two laws.** Beat 17 shipped WORDLESS: its narration contained the phrase "in a single camera move: the angel's finger..." and parse_script's MOVE_RE uses search() not a line anchor -- the parser ate the entire narration as a MOVE directive. One colon in 911 beats across four films. Fixed at source (colon -> dash), recompiled, relaunched (jubilees2). Banked: Law 25 (tag-words move:/visual:/motion: are FORBIDDEN mid-narration until the parser is line-anchored; empty-narration + tag-word checks go into audit/intake -- the box's wordless verify caught what four session-side gates missed = gate-parity gap). **v2 is immune to this entire bug class by construction: no .md, no prose-vs-directive ambiguity.**

**3. V2 BUILT: all nine files, in one session, from the standing design docs.** shared/v2/: schema.sql + migrations/0001_init.sql, db.py (spine: create/connect refuse-wrong, pending(), mark(), log_generation(), status_counts()), ingest.py (the one-way door: CSV set -> <slug>.db, method decided at door, canon snapshot, default 'main' EDL, re-ingest = polite no-op), render.py (thin orchestrator, --status reads truth from data), audio.py (Inworld verbatim, voice from project row -- channel.json walk GONE), align_with_whisper.py (carried VERBATIM -- the Troy-drift difflib fix), measure.py (whisper + alignment -> audio_start/audio_duration/word_timestamps per beat -- the per-word times v1 threw away, now kept), visuals.py (method registry floor/kling; ken_burns_still verbatim incl. duration fix + move doctrine; animate_still verbatim; kb-tail carried verbatim from assemble_episode and made THE ONLY path -- **Peter's ruling B, permanent: setpts appears ZERO times in v2; the never-stretch law is enforced by nonexistence**; content-policy refusal falls back to full-length KB not a held frame; ref-conditioned path fails loudly by name until Elijah wiring lands), assemble.py (pure EDL concat + music bed + sidechain duck verbatim; kb-tail apparatus deleted from assemble by construction), upload.py (OAuth/resumable/thumbnail/captions verbatim; the DB IS the header; video_id written back). ~1,375 new lines + 323 carried, replacing ~4,000+ across eleven v1 files. Landed via named-path commit 2eb6996 with *.db gitignore in the SAME commit.

**4. V2 PRODUCTION-PROVEN same session: the Thomas teaser.** A 21-beat, 802-word (~5.35 min actual) Part-VI teaser authored fresh (river -> chop -> gates -> src set -- NO csv2script, NO parse_script: first project ever authored straight for the door), ingested on the box, narrated (Elliot -- capital E; the lowercase default 404'd Inworld: paid-API resource strings are case-exact, Law 26), measured for real (98.5% coverage on genuine Whisper noise -- 'bear as knife blades', '1600' as digits -- exactly the divergence class the verbatim difflib core absorbs; 21/21, zero floors), rendered, assembled, and **UPLOADED PRIVATE -- appeared in Studio. v2 is proven CSV -> Studio end to end on its first real run.** Pilot also caught: measure.py's Path('')-resolves-to-'.' guard bug (whisper tried to transcribe a directory), the voice-case default, and the speaking_rate drift (channel 0.97 vs v2's 1.0 -- rate column pending). Small-N authoring learning banked (Law 27): at ~20 beats the 15% tableau cap = 3 beats, so teaser authoring is MONTAGE mode -- one route per token; and a river with no act headers routes everything to block 0 (chop needs headers).

**Fix-commit queue opened (engineering, next session):** measure.py is_file() guard; ingest voice default 'Elliot' + speaking_rate column read from src or arg; audit/intake empty-narration + tag-word hard checks; parse_script line-anchor patch (patch_*.py, v1 bugfix-frozen allows it); chop cross-block boundary unification (three axes now documented: tableau/shot-dist/human-absent all reset per block); select_thumbnail_still leg still erroring (standing backlog); authored per-beat `kling` column in ingest (Law 28e placement-by-value — gates the Bible-epic allocation). **ADDENDUM same day: teaser Studio verdict in — imagery loved, two front kling beats flagged as AI slop (contact-physics morphing) -> Law 28 banked (never simulate contact physics; continuous media only; events in cuts; witness grammar + physics anchor; dream license scoped by register map; kling placed on phenomena). **Jubilees Studio verdict same day: shipped and available; beat-1/beat-2 cold-open stills flagged (farmland aerial, literal crop-fire) -> Law 29 banked (THE MUTE-STILL TEST: every cold-open/hero variant spectacular as a silent frozen frame; metaphor substrates rewritten otherworldly; no more exploding farmlands).** Bible-epic flagship package proposed and signed off in principle (Sacred Dawn, ~3:15, ~29k words, ~730 beats, kling-by-placement ~90 clips at the $100 retention allocation).**

**Docs shipped this session:** SESSION-NOTES-2026-07-31, v2-PLUMBING.md (nine-file teardown + dependency map + CSV<->DB instruments + backlog folded in), _CANONICAL.md updated (Laws 24-27 + v2-era section), _BRIEFCASE-INSTRUCTIONS.md (the operating manual: process is entirely Claude; Peter runs laptop/box + Studio taste), ELIJAH-TASTE-INPUT.md (fal.ai playground -> canon -> door workflow, scene-contract framework, 24-model pillar-of-fire bake-off analysis, the 2-hour Elijah epic plan).


### 30 July 2026 — PART III SHIPPED + STUDIO VERDICT: BEST YET. MOTION THREAD CLOSED. ARCHETYPE LAW BANKED. BATCH-FORWARD STRATEGY ADOPTED.

Part III (Parables of Enoch, 310 beats, 12 acts, ~72 min, kling-12, $30.84) rendered and watched in Studio same day. **Peter's verdict: best film of the series so far** — pausing works, motion on stills varied and correctly scaled, set/subject variety across the film present. This closes the motion thread definitively: the duration-order patch confirmed in the wild at kling-12 (298/310 beats on the floor — the strongest possible field test), and the pure-distribution move law (`assign_moves_fuzzy`, banked as Law 23) visibly working on screen. Worry's motion mystery closed by explanation (kling-coverage ratio), no fourth bug.

**Two named repetition complaints in the same verdict produced Law 22 (composition-archetype):** ~80 beats of "seated authority above inferior figures" spread across FOUR different throne tokens, and 22+ of "man reads old document by fire with onlookers" — every per-token gate passed while the viewer saw one composition wearing four names. Root cause owned: variant banks were composition-homogeneous (every throne variant another full throne tableau). Fix banked and tooled same day: archetype tags per token in the chop config, archetype-share report + cap in `chop_river.py` (untagged tokens flagged too), and the variant-bank spanning rule (≥3 composition classes per token). Effective from Part IV.

**Strategy set:** batch-produce through at least half the 20-spoke slate and forward-schedule publishing (v1 `run_batch.py` already supports `--publish-start` + `--publish-interval-hours`) for daily coverage, decoupling the publishing cadence from the v2 build — build carefully, never under ship pressure. Marginal cash per 90-min film now ≈ two Heinekens and a calamari; the binding constraints are session-attention and distribution, and distribution is Peter's lane.

**v2 design artifacts banked this cycle (design only, build not started):** schema draft 1 (`v2-schema-draft1.sql` — project/beats/canon/generations/edl/meta, golden principle: DB + assets = deterministically reconstructible video), the v1 decommission map (freeze/quarantine/attic discipline, nine engine files die + two organ donors), and the scope+backlog ruling (v2.0 = parity with corrected v1; shots/scenes/coverage layer banked as backlog #1-2 with observable triggers; one open call: kling-tail stretch vs 15-line kill). Beats→shots→scenes coverage doctrine captured in the scope doc. First build step when it starts: stages 1+2 standalone; blocked only on `align_with_whisper.py` re-upload.

Part IV (Book of Giants) authoring begun same day under the new archetype discipline; Part V to follow in sequence.

### 29 July 2026 (evening, second session) — PART III PARABLES OF ENOCH: GENERIC TOOL PROVEN, FULL GATE CHAIN GREEN, LAUNCH-READY

Second Sacred Dawn session of the evening, requested end-to-end: build the generalized chop tool proposed in the Part II teardown, then use it for real on Part III rather than leaving it as a description. `chop_river.py` written (engine: sentence-aware chunking with hard word-cap, ordered-regex token routing + override list, per-token visual-variant cycling with human-word-forcing and distance-repeat guard, scale/move/weight/topic_class/air assignment, kling front-N slicing, CSV writer) with all project-specific data (token routes, visual variants, canon descriptions, kling motion prompts) supplied via an external `config.json`, not hardcoded — proven generic by authoring Part III's entire config from scratch, independent of Part II's.

**Part III authored river-first**: The Parables of Enoch — the spoke where Angle #1 (Son-of-Man parallel to the Gospels, reserved since Part I) finally unlocks. 8,041 words, cold open (a fourth genuinely distinct Opening-Signature-Ledger vantage: in-medias-res throne-room judgment, not Part I's ground-level tribunal or Part II's orbital gates) through six acts, closing on the out-hook already promised to Part IV (the Book of Giants). Deliberately shorter than Part I/II's ~10-11k precedent — a real, flagged effort/length tradeoff to keep the full pipeline moving in one sitting, not a new target. Opener-variety self-checked and fixed from the first draft this time (max 3/66 paragraphs sharing an opener) rather than needing a dedicated correction round.

**Angle #1 handled under the series' own attribution-moat discipline, not oversold:** the film states plainly, and states BOTH sides of, the genuinely live scholarly dating debate for the Parables of Enoch — internal evidence (the Parthian-invasion reference, silence on the 70 AD Temple destruction) pointing some scholars toward a pre-Christian or contemporary date; the section's real, confirmed absence from the Dead Sea Scrolls (every other part of 1 Enoch recovered at Qumran, this one not) pointing others toward a later date or independent transmission. The film draws the honest, defensible conclusion — a pre-existent, throne-seated Son-of-Man figure was demonstrably alive in Jesus's own religious world, whichever way the dating argument eventually resolves — rather than the stronger, unsupportable "proves Jesus copied Enoch" claim the angle table exists specifically to guard against.

**Real gate chain, second time this session, both genuinely re-run rather than assumed from Part II's pass:** `chop_river.py`'s first output immediately surfaced the same class of issue Part II needed two manual rounds to catch — one token (`son_of_man_throne`) at 29/208 beats — caught and partially rebalanced via config edits (new tokens + force-reassigns) before AUDIT ran at all, this time, rather than after a warning or a manual histogram read. Real `audit_script.py`: three negation-phrase hard-gate failures found and fixed (the tool's own generated variant text, not river prose), then a clean pass with the same class of expected warnings as Part II (kling-band, DIAL-2 span, residual near-duplicates). **Proactively scanned for the audit-vs-compile word-count gap (Law 21) before attempting COMPILE this time** — caught 2 of 3 total over-cap beats before the first compile attempt; the third slipped through the proactive scan (a different sentence than the ones checked) and cost one real `COMPILE FAIL`, fixed immediately, second compile attempt clean. Real `parse_script.py`: 208 beats in, 208 out, exit 0, Mode A 208/208.

BOM at kling-12 (held to the standing law this time, not continuing Part II's one-off kling-40 hedge, since that hedge's outcome isn't yet confirmed): 208 stills × $0.08 + 12 kling × $0.42 + $1 ≈ **$22.68** — under the $25 default, no override needed.

Full deliverable (`-src/` set + compiled `out/` + `chop_river.py` + its config) packaged for handoff. `_CANONICAL.md` §1 updated with the new tool entry. See the session's own handoff block for the launch commands.

### 29 July 2026 (evening) — PART II ASTRONOMY BOOK: FULL GATE CHAIN RUN FOR REAL, THIRD MOTION BUG, HANDOFF PROTOCOL CORRECTED

First session with the complete gate chain actually runnable in-container: `audit_script.py`, `csv2script.py`, `parse_script.py` all executed for real against real files, not simulated or assumed. Authored Part II (The Astronomy Book of Enoch) river-first (~11,000 words, calibrated against Part I's actual 11,432-word draft rather than an arbitrary target), then chopped to 274 beats. **Cold open rebuilt from scratch mid-session:** the first draft, following the condemnation-family law literally, restaged Part I's actual shipped town-square/tribunal imagery almost beat for beat — caught only because Peter had Part I's real screenshots open beside it. Replaced with a cosmic/orbital vantage (twelve gates opening along a planetary horizon, collapsing to one human figure) per Peter's own shot-by-shot brief, with the ancient-aliens-adjacent "bronze rings/computer" language deliberately stripped to hold the line on the permanently-banned Angle #9. Graduated to a new standing law, the **OPENING SIGNATURE LEDGER** (`_CANONICAL.md` §3): the condemnation-family law governs a feeling, not a scene; every spoke's actual cold-open concept gets banked and checked against future spokes. Five additional banned images named and banked series-wide (ironmongery/forge, dusty scrolls/bookshelves, lectern books, more-than-one village-wide or oath-mountain shot, sand-drawing-with-shadow scenes, more-than-one Hermon) — real rule underneath: each spoke draws only from its own spectacle inventory, never borrows a prior spoke's signature images as filler.

**A THIRD motion bug found and patched**, distinct from the morning's two (`_CANONICAL.md` §7 has the full trace): `cmd_finish`'s tiered-render loop ran before narrate/Whisper-align on every first-time render, so `_tiered_duration()` always got `None` (`durations.json` is never written anywhere in the codebase — confirmed by grep) and every floor clip baked at a hardcoded 5.0s fallback regardless of real beat length; `assemble()` then stretched each undersized clip 3-4x to match, and a 16% zoompan spread that thin over that much extra time reads as static even though motion was technically present. Also found: the existing manual `durations.json` workaround was mis-pathed one directory level. Patched (`patch_finish_duration_order.py`, idempotent, anchor-verified, applied on the box), plus a new working hypothesis for Worry's still-unexplained real motion (kling-coverage ratio, not a fourth bug — cheap check pending, not a new debugging thread).

**Real audit-driven fix loop caught three additional, smaller genuine bugs before spend:** a canon token with digits in its name (`calendar_number_364`) silently broke `audit_script.py`'s own token-stripping regex, collapsing three beats' shot-scale classification into a false "other" bucket and tripping a hard gate that had nothing to do with the actual content — renamed, banked as Law 20 (canon tokens: letters/underscores only). Five beats passed `audit_script.py`'s word-count check at exactly 55 words but failed the REAL `csv2script.py` compile at 56, because audit's check filters `"--"` before counting and the compiler's doesn't — banked as Law 21, flagged for the change queue (fix one tool or the other, not yet decided which). A literal typo in `canon.json` (`sun.s` for `sun's`) was caught only by reading the actual compiled `.md` output, not by any gate — would have shipped silently into image prompts otherwise.

**Token-overuse fixed in two rounds, both by Peter's manual eyeball, not by any tool:** `gates_wall` 40→7, `sky_ancient_science` 30→7, `qumran_caves` 18→2 (round one); `calendar_grid` 32→9, `bound_stars` 23→9, `priest_power` 19→9, `enoch_uriel` 18→10 (round two, requested after round one). Confirmed mid-session that `intake.py` — the tool `_CANONICAL.md` §3 names as the actual tableau-budget enforcer — has never been present in any briefcase this or prior sessions have seen; `audit_script.py` alone does not catch per-token share. Proposed fix banked in `_CANONICAL.md` §9: a hard-cap check in `audit_script.py` itself rather than waiting on `intake.py`.

**Real COMPILE + MACHINE GATE, both green:** `csv2script.py` (sourced from a 26 Jul briefcase after the 29 Jul briefcase was found missing it entirely) compiled 274 beats, 12→40 kling on request; `parse_script.py` returned 274 beats in = 274 out, exit 0, Mode A 274/274. BOM at the standing kling-12 would have been $27.96 (over the $25 default, in line with Part I's own approved $28.44 override); Peter deliberately chose kling-40 instead — a one-off hedge against the third bug recurring, not a new default — landing at ~$39.72. **Launched same session**, `tmux` session `astro2b` on the box, running unattended via `orchestrate.py`.

**Handoff protocol corrected mid-session.** Peter invoked "Law 15" to stop a pattern that had crept in: delivering the launch sequence as several separate turns (plan-check in one message, launch withheld for the next; then a spurious pre-launch collision check for a run that had never fired) instead of one complete block. Law 15 amended in `_CANONICAL.md` to state explicitly: once COMPILE + MACHINE GATE pass, the entire remaining command sequence — scp, inbox+plan-check, AND the tmux launch — goes out in one message, with any stop-condition riding alongside the launch command rather than gating it. Peter does not review anything between launch and the finished video in Studio; that was this session's misreading of the process, not his actual requirement.

Full narrative + diagnostic trail: `SESSION-NOTES-2026-07-29-astronomy-book-enoch-part-ii.md`. Claude's own reasoned process retrospective (requested separately, not a session-notes substitute): `SESSION-FEEDBACK-2026-07-29-process-teardown.md`.

### 28–29 July 2026 — THE RIVER-FIRST PIVOT + BIBLE THEY BURNED LAUNCH (Sacred Dawn)

Sacred Dawn re-pointed from the Pools-A/B universal-question slate to a new 20-spoke hub-and-spoke series, **"The Bible They Burned"** (teardown of the channel's own #1 video: 6.4% CTR, 18:06 AVD on 76 min, 13.9% end-retention). Authored + shipped Part I (Enoch: Fallen Angels) via a new default authoring sequence, **river-first**: pure narrative draft, then chop into beats — replacing simultaneous CSV+prose authoring after a "bottles not river" repetition diagnosis (token-novelty checks catch literal repeats, blind to compositional ones — three tokens secretly the same shot). Doctrine graduated to `_CANONICAL.md` §3 (Law 16; the READ step at §4 step 2.5; two-zone cold-open scale, Law 17; thumbnail-payoff, Law 18; $30-as-override not new default, Law 19). Cold-open signature retired series-wide: concealment-family (floor stone/shelf/night road) → **condemnation-family** (daylight bonfire/tribunal/named heresy proclamation). Kling held at 12 through the authoring-method change on purpose — single-variable falsifier discipline. Ten-angle suppression-narrative reference table banked in the slate (Nicaea-myth and ancient-aliens angles permanently banned series-wide; Son-of-Man material reserved for Spoke III). **Engine archaeology (unplanned, real cost) — two motion bugs found AND FIXED same session:** `modea_beats.py` dropped the `move` field on the modea render leg (built for a different channel's schema entirely, never had a `move` concept); `ken_burns_still`'s push/pull zoom froze after 5.6s on longer beats. Both patched (`patch_modea_beats_move.py`, `patch_kenburns_duration_scale.py`), applied on the box, and verified on real re-rendered clips pulled and eyeballed directly — genuine motion confirmed, not just a clean isolated test. Fixed on the existing river Enoch project with zero new spend, via a recipe now banked as reusable doctrine in `_CANONICAL.md` §7. A THIRD, real, pixel-confirmed motion mechanism (Worry, Scripture On Screen) remains genuinely unexplained by either fix — do not assume it's the same root cause. → `_CANONICAL.md` §7. Open decisions parked, not resolved: angle-#5 (simpler-Satan) in/out; 8-vs-7 chapter count as standing exception; Worry follow-up needs its own session. Full narrative + diagnostic trail: `SESSION-NOTES-2026-07-28-29-river-first-enoch-redo.md`.

### 24–25 July 2026 — THE FULL-CIRCLE SESSION (falsification → pivot → THE BRIDGE → chambers fired → the new canonical → the docs tree)

The largest session on record and the first complete flywheel revolution. Full narrative: `docs/sessions/SESSION-2026-07-25-full-circle.md`; quick reference `SESSION-2026-07-24.md`. Headlines: **(1)** the 112× packaging thesis FALSIFIED by Covenant Lens (winners-only analysis = survivorship bias; method law banked) → lottery model (never pause uploads, judge N≥40). **(2)** **Universal beats lore at N=5 operators** (100–800× within-operator, incl. a winner whose Enoch content is its own worst; paired Kasdeja failure across two operators) → the topic gate + the `topic_class` ledger column. **(3)** The **rung ladder** instrumented: rung 1 (~300) = run-rate × time (FeelAngels/BSHH controls); rung 2 (monetized) = grind (Sealed Word, 8 subs/vid) or **breakout (Bible Academia, 24 × 50-min universal-question films, 304 subs/vid)**; frozen cohort in `docs/_PACKAGING-AUDIT.md`. **(4)** The **retitle wave shipped**: `packaging_push.py` built (dry-run default, full-snippet echo, revert JSON, bound-channel guard); 19/20 live in universal-question form; channel keywords + country→US. **(5)** The **40-title slate** authored (`docs/slates/`), Pools A/B front-loaded; length doctrine: **cost = words-per-beat** (golden-pair ~45w ≈ $32/hr vs LEGO 13.5w ≈ $104/hr); slate default 60 min, ledger-gated. **(6)** **THE BRIDGE built + proven in one day, zero engine changes:** `audit_script.py` + `csv2script.py`; the **GOLDEN PAIR law** (bible-they-burned-v2 = format authority; format LAW / content INCIDENTAL; conformance = the real `parse_script.py`, beats==rows, zero warnings — the oracle caught three emitter faults before any spend). **(7)** **`chambers-of-the-dead` through the bridge:** dials-at-Step-1 architecture passed everything Methuselah failed (spectacle 82% vs 12%, novelty in every block, zero smear, spine 3.0% vs cap); 10 cold-open rows authored per §8; probe ($1.60) went full end-to-end, register HOLDS, and bought two rulebook negatives (letterbox family + armour family, `patch_sd_negatives_probe2.py`, sword excluded for Eden); **real run fired, 330 beats, `--kling-count 40`, ~$43 — mid-render at close.** New laws: the **`-src` convention** (create_project owns `<slug>/`, refuses if exists) + **commit the -src rename immediately** (the engine auto-commits projects on the box; uncommitted renames become recorded deletions — happened, recovered). **(8)** **`docs/_CANONICAL.md`** ("crank the handle · best of both worlds · rung by rung") replaces the old canonical; Appendix A = the hand-clocked five-station process (whiteboard walk); Appendix B = the fresh-session ritual. **(9)** **The docs tree:** 122 scattered .md → `docs/` (channels/slates/scorecards/sessions/archive); `shared/docs` DISSOLVED (backup sediment deleted, git history retains); box settled its historical deletions; both machines byte-identical. Pass 2 (channel-local docs folders) = own session.

### 07–23 July 2026 — THE GAP (worklog unmaintained; compressed honest catch-up)

Seventeen days of heavy work never logged here — detail lives in the doctrine changelogs (`docs/_LEGO.md`, `docs/channels/_Sacred-Dawn.md` §7b) and `docs/scorecards/METHUSELAH-scorecard.md`. Compressed: **bible-they-burned-v2 authored + launched** (76 min, 237 beats — became the channel's best batch video and, on 25 Jul, the named GOLDEN PAIR); **Sacred Dawn batch-2 repackaging** (63/66 videos live with bright-HDR packaging); **the LEGO system designed + built** (`_LEGO.md`: package-first, the subject ledger, the three dials, the grid + human pick, container-fill/calibrate); **METHUSELAH feature** shipped through it at $156 → scored **architecture 28 / execution 86** → the scorecard rubric was born and the core reversal banked (*run the dials at Step 1 on the ledger, refuse to author until they pass*); **WITW** shipped (the proven 8-block container + 13.5w density); register scrubs; **chambers designed 23 Jul** (package + architecture with dials-at-Step-1 — the design the 25 Jul session then fired through the bridge). Also in this window: `_Sacred-Dawn.md` §7b (CTR-starved-not-distribution-starved; the cold-open retention data 0.22–0.36 first minute) — written 23 Jul, one day before the session that confirmed it from the outside.


### 06 July 2026 — FLOOR-FIRST SHIPPED (engine + UI) + auto-assemble removed (MC v3.8 → v3.9.2)

The session that turned the 05 Jul floor-first SCOPE into shipped, proven code — engine and UI. Deep narrative: `SESSION-NOTES-2026-07-06-floorfirst-autoassemble-shipped.md`. Seven commits, all idempotent `patch_*.py`, laptop→GitHub→box.

**The ten-animal button-test rig** (`scripture-on-screen/projects/ten-animals-buttontest`, dog→rabbit, mixed narration lengths BY DESIGN — short beats leave inheritable tail, long beats force fallback). Each beat targets a mode scenario; proven at the artifact: Kling atoms (5.04s), KB override duration-exact (pig 8.13s > a 5s atom → can't black-frame), the inherit chain, and **the ballgame — rabbit inheriting from the sleeping cat (a KB source) correctly went RED and fell back FREE to Ken-Burns on rabbit's OWN still** (frame-0 of shot_010 = rabbit, not a slice of cat's clip). **"You can only inherit from a horse" proven at the artifact, on neutral footage, before Elijah.** Now the permanent floor-first/mode regression harness. **Also: the inherit ARTIFACT proof (05 Jul open #2) CLOSED** — horse→cow→sheep is ONE continuous move across both handoffs in the assembled cut (derived-clip design holds end-to-end; sync doesn't drift). Two-assembler footgun re-confirmed: CLI `finish --assemble-only` drifts (uniform-slice signature `uniform 57.8s/10=5.78s`); only `assemble_episode.py`/the MC button is aligned.

**Auto-assemble REMOVED (box-folder-is-truth, part 1 of 4).** The launch spawns `orchestrate.py`; `decide_legs` unconditionally appended convergence. Fix mirrors the stills-gate stop one leg later: `--stop-after-clips` (`f90ecf0` MC always passes it → v3.9), `clips_ready` as a clean terminal phase (`358f746` — was mislabeled "dead" by the pid reaper), and the assemble-at-clips panel (the chicken-and-egg: the assemble button lived only in the has_video panel, so a clips_ready project could never be assembled from the UI). **Peter's decision: ALWAYS stop at clips** — every `final_video.mp4` is now a deliberate press.

**FLOOR-FIRST engine (`059ad73`, proven at `--plan`).** Policy-only (no CLI flag — MC writes the policy, existing readers do the rest). `_kling_override_set()` mirrors `_kb_override_set()`; additive routing `kling if (bi in kling_override) or (bi < kling_count and bi not in kb_override)`; floor-first runs `kling_count:0` so ONLY the override list turns Kling on; the `--kb-floor` "pass" is just routing every non-Kling non-inherit beat to the existing `ken_burns_still` (duration-exact, free). **Delete-on-upgrade via a `.kbfloor` sidecar** (KB and Kling both write `shot_NNN.mp4`, indistinguishable on disk): the Ken-Burns branch touches `shot_NNN.kbfloor` (assembly globs `shot_*.mp4`, never the sidecar); a beat entering `kling_override` whose clip is marked gets clip+marker deleted so it re-renders Kling. **KB→Kling deletes a FREE marked clip; a paid atom has no marker so Kling→KB never deletes.** `--plan` proof: `{kling_count:0, kling_override:[0,5]}` → beats 0+5 Kling, other 8 free floor. Backward-compatible (no key = unchanged).

**FLOOR-FIRST UI (`a640fa6`, v3.9.2, proven at the click).** "Floor all (free)" button in the section bar → `/api/floor_all` sets `kling_count:0` + clears the three per-beat lists (proven: click wrote `{"kling_count":0}`). Motion presets **auto-enroll** (Peter's decision — under floor-first a preset that doesn't turn the beat Kling is a confusing no-op; picking a motion IS choosing to spend): a Dynamic/Slow-crane click also POSTs `/api/kling_override`, which drops the beat from kb/inherit (one mode per beat) and deletes its `.kbfloor` clip. Floor-first is now one click: floor everything to $0, add motion per-beat, widget ticks from zero.

**THE cssText AFTERNOON (the day's real moat contribution — banked so it never repeats).** The floor-all + assemble-at-clips panels first shipped with inline JS `cssText = "...color:#d4a017;..."` strings — a quote/escape collapse across the Python→JS boundary threw `Uncaught SyntaxError: Unexpected identifier 'color'`, hung MC at "loading…" for ~2h. `py_compile` PASSED (can't see broken JS). Compounding: `systemctl restart` didn't swap the orphan process holding :8002 (needed `stop`+`pkill`+`start`); a partial `git checkout HEAD~1` left the bug in with a reverted version line (grep read 16, not 0); the FIRST bad commit was the assemble panel (`6b9fef5`), not the floor-all button. **Recovery:** reverted to the last-clean page (`358f746`), committed the revert forward (`3f5da6f`), rewrote BOTH panels in ONE patch (`patch_floorfirst_ui.py`) with individual `element.style.prop=` assignments + a build-time no-cssText guard, gated the deploy on `curl localhost:8002/?key=... | grep -c "Unexpected"` = 0 BEFORE opening the browser. **Lessons → canonical §7: (1) click-before-commit — compile proves syntax, only a page-load+click proves the JS parses and the handler runs; we shipped four MC patches without it and the fifth cost 2h. (2) No `cssText` string literals in server-emitted JS — build DOM via `.style.prop=`, guard it in the patch. (3) `curl … | grep -c "Unexpected"` is the box-side JS-parse gate. (4) After a revert, grep the file — trust the grep, not the stamp. (5) `systemctl restart` may not swap the process — confirm the PID, pkill the orphan.**

### 05 July 2026 — MC v3.7–v3.8 (spend widget + section bar) + FLOOR-FIRST scoped + docs reconciled

Short continuation of the craft-vs-cash arc. **v3.7:** persistent ESTIMATED SPEND widget, fixed bottom-left, routing-exact (kling only if `beat < N and not kb/inherit`), opens at current spend and ticks live with every toggle, shows already-rendered → remaining. **v3.8:** sticky section-navigation bar — per-`stage` buttons render ONE project part at a time (ALL default ≤200 beats, first-section above; empty stage folds to "—"). This is **pagination disguised as navigation** and it is the **Elijah-scale prerequisite** — at 1000+ all-trim beats only the visible section's autoplaying videos mount. Cost widget grew a "section $ / project $" split under filtering.

**FLOOR-FIRST scoped here (→ SHIPPED 06 Jul, see that entry).** The design: auto-Ken-Burns EVERY beat the moment stills land → assemble-ready **$0** project → walk it section-by-section applying craft, spend ticking from zero. Inverts review psychology; unifies the $3-KB and $17-cinematic videos as one project at different depths. Built next session as the engine additive-routing patch + the "Floor all (free)" button + preset auto-enroll + `.kbfloor` delete-on-upgrade.

**Also discussed (not built): frame-chaining** for the minutes-long continuous scenes competitors run — the model still only makes 5–10s atoms; they feed clip A's last frame as clip B's start image with a continuing prompt (Kling extend / first-last-frame), 5–6 links = 30–60s, the tell is drift (faces/costumes mutate, often masked with slow-motion). Would be a fourth per-beat mode ("Extend previous, $0.35") sharing the inherit chain machinery but making a paid call from A's last frame. Doctrine question not capability — Synthetic's all-trim register is the opposite bet; candidate for Elijah emotional-peak flagship spend only, evidence-first (watch-tool teardown of a competitor before designing).

**Docs reconciled (05 Jul):** canonical `.pre_0702` was a month stale → updated to v3.8 reality (roster 11+ channels with correct statuses, §5.12 the full MC arc, four new doctrine blocks: patch-craft discipline / channel-fixes-in-channel-config / the fal silent-substitute family incl. baked-in letterbox / sources-persist-artifacts-derive, §10 current state 05 Jul). STARTUP_PACK confirmed dead and de-referenced.

### 04 July 2026 (evening) — THE CRAFT-VS-CASH SURFACE (Mission Control v1.9 → v3.6)

One sitting: both §7 per-beat cost-controls SHIPPED plus everything the building surfaced. Twelve MC versions, three engine patches, ~15 idempotent anchor-verified patch scripts, two hotfixes, one ~$3 test project (`kb-toggle4`), ~17 doctrine entries. Deep narrative: `SESSION-NOTES-2026-07-04-mc-craft-cash-surface.md`.

**The prerequisite read paid immediately:** `assemble_episode.py` proves **sync is structurally safe** — `durations.json` is the sole timing source, VOICE WINS, clip file-length never becomes timeline → both toggles change only the pixel source by construction. Gettysburg side-read: beats average ~2.2s vs the 5s atom — **half to two-thirds of every paid clip was being discarded** (clip-merge is the bigger lever).

**KB override** (`kb_override` in `render_policy.json`; slot SAVED not slid; `--plan` marks; proven zero-cost on the real film) **flushed a portfolio-wide engine bug:** the 01 Jul QQrew hardcode had turned EVERY channel's KB floor into a held frame (channel doctrine silently became engine law — build-order bias, new head). **Fixed at the root:** flag resolved from the STILL's own path (CWD-independent, uncached); QQrew's true-static floor preserved, cinematic channels get the capped zoom back. → **channel fixes live in channel config; if the flag doesn't take, fix the resolution, not the constant.** Mixed test results then exposed the second law: **MC imports engine code — restart after every engine pull, never mid-render.**

**Clip-merge shipped as INHERIT, the derived-clip design** (Peter's risk instinct reframed it): beat B renders nothing; an inherit pass manufactures `shot_B.mp4 = ffmpeg -ss <offset>` on the source atom — **assembler and `_index.json` untouched**, chains walk back to the nearest non-inherited ancestor summing frozen durations, every failure (no predecessor / missing source / <0.3s left, ffprobe-guarded) falls back FREE to `ken_burns_still` on B's own still. **UI mirrors the render pass exactly:** "Inheriting beat J - chain of N beats on one atom = Xs" — green fits / amber tail-falls-back / **red when the source is KB** ("you can only inherit from a horse, never from a wheat field"), live-repainted on every click. Mutual exclusion KB↔inherit server-enforced. **OPEN: the artifact proof** (rm one inherited clip → `--animate-only` → Re-assemble → one continuous move across the boundary).

**The mode system (emergent):** Dynamic / Slow crane-up presets (exact Gettysburg wording — the slow-crane line measurably cut hallucinated animation) grew into **the mode invariant** — every beat visibly in exactly ONE mode (Kling = green box border + matching preset; KB; inherit), presets are **mode buttons** (one click from any mode to Kling, released through the real endpoints). → **three-tier motion rule for §7: "animate the galloping horse, never the wheat field."** Dead Accept/Reject judge buttons removed (in-memory only, no consumer; reject-list batch-rerender banked deliberate-later).

**Also shipped:** scroll-to-top (⇧ Top, #33); **thumbnail source-modes** (#32 superseded-better: upload = persistent `thumbnail_source.png`, Generate branches `--shot N` / `--still <source>` — `make_thumbnail.py` already had the flag, ZERO engine change → **sources persist, artifacts derive**); **letterbox Analyse+fix** (fal family += baked-in bars inside a correct canvas — `enforce_16x9` can't see them; strict near-uniform-pure-black detection protects dark cinematography; backups in a `_pre_letterbox_fix/` SUBDIR so stills globs never match; flags fixed shots that already have clips); the ship panel rebuilt **CONTENT | PACKAGING equal boxes** (metadata rides with the video; the layout is the CTR+AVD thesis drawn — after a four-deep width-cap archaeology: `.panel` class → inline → slot → grid; **grep the whole cascade first**).

**The patch-craft family (two page-killing hotfixes, both banked):** (1) a single `\'` in a tooltip — the escape survives the patch layer but the server's own parser collapses it inside the Python-string-carried JS → dead page; **no apostrophes across the Python→JS boundary**. (2) `shutil` NameError — imported in the patch, not the server; **handler imports live IN the handler**. Umbrella law: **`py_compile` proves syntax, never names, never carried JS → MC verify = compile + load the page + click the button.** Plus: **every MC patch bumps APP_VERSION** (the stamp caught a silently-untraveled patch AND a stale-Downloads apply — after amending a patch, re-download). Housekeeping: `reassemble_static.py` + `make_thumb_bottomleft.py` rescued from untracked; the ffmpeg-assemble-fix priority turned out already committed. Authoring: **parser section-marker whitelist** (`## COLD OPEN`/`PART`/`ACT` only) → `_Synthetic2.md §5f`.

### 03–04 July 2026 — THE GETTYSBURG LAUNCH (Synthetic Press goes blockbuster) + the Shorts leg

Two days, boardroom-relic → live blockbuster channel. **SHIPPED:** GETTYSBURG **trailer** (45s, 7pm NY on Pickett's-Charge 163rd anniversary, AVD 94.8%) + **the full film** (185 beats, **7:03**, poster thumbnail, real chapters, live 9am NY 4 Jul) + **~30 vertical Shorts** (first-phrase titles, square-blur, hourly through 5 Jul). Channel branded/verified/funnel-wired. Full craft in `_Synthetic2.md` v1.2 + `SESSION-NOTES-2026-07-03-gettysburg-launch.md`.

**The method proven (the point of the whole exercise):** the doctrine retrofit works — a 12-channel pipeline re-aimed at a NEW register (Bay/Woo blockbuster) shipped a trailer + feature + 30 Shorts in ~2 days, with the operator fixing ~a dozen frames by hand. The playbook is now a template; **the second blockbuster channel costs a day, not a week.**

**Authoring doctrine banked (`_Synthetic2.md §5`):** all-trim by construction (11-word beat ceiling → every clip trims, zero stretch); the **number-density split model** (spelled numbers + mid-beat stops read ~0.6 s/word vs 0.36 — split number-dense multi-sentence beats); the **six-slot VISUAL grammar** (camera-position-only/no speed words, period anchor every human beat, geometry stated, three-plane depth, one BRIGHT light clause, evidence-only negations); **faces-as-required-instrument** (anonymous faces can't drift, named figures backlit, no gore, youth-nouns trip the filter); per-beat diegetic atmosphere (murk never in identity); the **8:01 mid-roll law**.

**Economics measured + the cost-control insight (§7):** **$12–13/min fully loaded** (first-film regen tax), ~92% Kling, all-trim = ~2.2× footage overhead; trends to $9–10 by film three (learning loop visible as unit cost). **The two MC cost-controls scoped** — Ken-Burns-override (static beat → free push, also safer) + clip-merge ("previous covers this" — render nothing for the second of two short same-scene beats, recover trimmed footage, kill that beat's re-render tax). **The review page is where craft-judgment meets cash per beat; the gate is narrative-function, not a global rule.** Binding constraint stays gate-attention, not dollars.

**Packaging laws:** movie-token title ("— The Movie (2026):"); **two grammars** — gap (hook copy) vs **poster** (one massive proper noun = series signature); thumbnail frame-selection (reaction beats calm portrait; arousal beats art-house grief; frame must promise the TITLE's video); funnel-pair escalates never repeats. Thumbnail infra: new channel needs a cinematic `thumbnail` block or renders on raw defaults; subtitle colour is RGB-list not hex; **custom thumbnails need channel phone-verification** (the silent-fail root cause).

**Strategy corrected twice on evidence** — (1) **cold-start physics:** first-video millions ARE possible with no audience (borrow the topic's suggested graph, win the impression auction on CTR+AVD; subs=floor not ceiling); one launch proves nothing either way; answer to variance = volume on a coherent cluster. (2) **"empty lane" WRONG** (Peter's memory beat a narrow sweep): the biblical AI-movie lane is a served, industrialized, **rising wave with multi-winner-tolerance** (same story, competing operators, all win millions — pre-sold IP). Third competitive situation banked (not empty-lane, not weak-incumbent). **Niche-validation rule hardened:** every calendar slot gets four demand gates + the operator screen BEFORE scripting; docs carry the EVIDENCE not the conclusion.

**The biblical pivot scoped (§13):** SOS → blockbuster; pilot **Elijah** (double-validated 2.7M+3.0M; demand map inverts the Sunday-school canon — Elijah 2.7M vs Moses 248K, same operator, 11:1; mystery+dread+magnitude beat fame). Bridge 25–35 min first. Prerequisites: **per-channel reference-lock** (engine lock is QQrew-registered → Elijah renders Pixar-cheerful) + parallel-fal semaphore. Apocryphal cluster (Enoch/Nephilim) is the hottest but collides with SOS's canonical charter → routes to Sacred Dawn.

**The Shorts leg (§14):** film = Shorts quarry; **title = first spoken line verbatim**; **square-centre-blur** vertical (1080² sharp centre + blurred fill — honest fit for cinematic wides; subject-centred beats may full-bleed); first-phrase via local faster-whisper. Cadence caution: hourly-into-one-feed saturation risk (thin next run if later Shorts throttle). The read is the distribution of all 30, not any single Short → gates `make_shorts.py`.

**New failure classes banked:** fal **dimension-clamp** (request >1440 wide → silent 4:3, no error → 1280×720 canvas + standing dimension sweep); **youth-noun black-frame** ("drummer boys" trips the filter → render the object not the minor); **runaway-motion on unspecified-static beats** (a "powerful momentum" default drove a parked wagon off-frame → tell Kling what MOVES and what STAYS, or put the motion source in-frame). Verify-at-the-artifact reinforced (fal silently substitutes: black PNGs, clamped dims, held clips). **Ops:** run commands from `~/Pipeline` not `~` (bit 3×); fingerprint the intended script version on the MC panel before Launch (v5-vs-v7 paste incident); chapters from real `durations.json` never estimated (head-offset check if the edit suite adds time).

**FLAGGED:** the auto-loaded operator profile / STARTUP_PACK is weeks stale (5 channels, "Synthetic launching") — refresh so sessions stop loading old state.

### 02 July 2026 — @Q-Qrew Ep5 (FIRE) SHIPPED + the anonymous-human failure class CLOSED + the flat-colour thumbnail method

Brain's solo debut. **Fire** ("Why Early Humans Would've Died Without Fire") — 155 beats, ~6-7 min, uploaded private to @Q-Qrew. First episode authored FROM doctrine, not memory. Project `qqrew/projects/fire1/modea` (the `1` suffix = the v2 grammar rewrite; `fire` = dead v1 — **fingerprint projects by content, never by name**, bit us twice now incl. maracanazo1).

**THE HEADLINE (the core lesson, now banked both layers):** Fire first-rendered with ~25 stills as MODERN CARTOON PEOPLE — beanie kids, cafe strangers, one beat put the real scene in a thought-bubble. Root cause: **NB2 text-to-image renders any UNANCHORED human as a modern cartoon character**, and the rulebook `people_directive` was being stapled onto person-free beats whose wording ("no face", "no clear animal") slipped the narrow phrase-guard. **Closed on BOTH layers:** (1) ENGINE — `patch_nopeople_default`: on a reference channel, a text-path beat has no ref → is person-free by definition → strip the people_directive unconditionally (cannot be defeated by wording); (2) AUTHORING — the v2 rewrite removed every anonymous-human beat (→ crew `/edit` beat or person-free landscape, narration carries the humans). **This is the moat pattern: a failure closed on the engine layer AND the authoring layer is structurally impossible to recur. Fire is the last script that can hit this class.** Banked to `_QQrew.md §6b` + canonical §8.

**Diegetic-teaching upgrade (fell out of the bug):** abstract flat teaching-graphics JAR against immersive scenes → teaching is now IN-SCENE by default via crew body/props — fingers, sand/dirt-writing, twigs, and ★ **Brain's FIELD NOTEBOOK** (jots a finding, holds to camera; her signature). Notebook is garble-tolerant BY DESIGN — hand-drawn field-notes are meant to be loose, so NB2 text-garble reads as authentic. Proven: the notebook renders were a highlight.

**Thumbnail method PROVEN — direct-render on flat colour, NOT cutout-composite.** We over-built first (a `solid_color_character` rembg-cut+composite mode, 4 patches, positioning bugs) — then Peter's question "how did the reference thumbs get it perfect?" reframed it: they were generated directly ON the solid colour in one shot. `make_character_ref.py --ref brain_ref.png --prompt "...on a solid flat [colour] background..."` → existing `low_silhouette` text overlay. Excellent result. The cutout mode is DEAD CODE (retire); `--composition`/`--bg-color` CLI flags kept. Banked `_QQrew.md §8`.

**Brain fully specced:** Lauren @1.0 EXPRESSIVE; discovery epistemology (arc engine: Driver=action, Skeptic=proof, Brain=discovery); inward self-deprecating humour ("that floored me") vs Skeptic's outward; contact beats + notebook; grandma-texture (1/ep); canon warmed vs the flat-expression trap.

**Also banked:** the 01 Jul "all-NB2" decision was DOCTRINE-only — `image_model` was still v1 in config until tonight (`patch_nb2_text` implemented it). Ops lessons: anchor patches to LIVE box code (a patch REFUSED tonight on a stale anchor — the check saved us); engine caches config+module at launch (relaunch/restill fresh to pick up patches; never restart the review service mid-render — cgroup teardown kills the run); `restill --project <dir>/modea --shot N` uses ENGINE shot index, not the MC gate's beat-label; Fix-this-image repairs defects but can't beat a genre prior or fix object-word language.

**OPEN / NEXT:** MC "Generate 5 poses" button (design locked — direct-render path); Salt (ep6) held for Fire's 48h CTR+AVD, inherits full grammar → should render clean first-pass; fold the two `_qqrew_*_DRAFT.md` into `_QQrew.md` and delete; retire `solid_color_character` dead code; confirm upload API 2nd-thumbnail (prior: no, Test & Compare is Studio-side).

### 01 July 2026 — @Q-Qrew Ep3 (MERCATOR) BUILD + THE STYLE_SUFFIX DAY

The day the flat-cel doctrine died. Scrapped the contaminated pregnancy1 Ep3 (stylistic grab-bag) and wrote a fresh **Mercator** episode ("The Map On Your Wall Is Lying — Greenland Is Not That Big") — Skeptic solo-lead, 180 beats, ONE coherent visual world (maps only) to structurally prevent the grab-bag failure. 62% Skeptic presence (carries the episode, bookends fixed), crew-absent wides forced people-free to stop the model inventing strangers.

**THE HEADLINE (cost a full day, now banked everywhere):** the Ep3 photoreal-portrait "disaster" was MISDIAGNOSED in the §11 P0 spec. Reading the live code proved the `style_suffix` WAS reaching prompts — the real culprit was its CONTENT: a flat-cel/webcomic string that hard-banned "painterly, semi-realistic, rendered, 3d" and forced a cheap kids-cartoon on every text-to-image beat (the Bambi fawns, the flat skies). **Fix = swap the suffix** (flat-cel → semi-realistic bright). Immediately produced the approved trio-tier look. The canon-tag cut (89w→18w) shipped too but was minor — Skeptic beats route through `/edit`+PNG and bypass the canon. **Second trap same day:** the first swap over-corrected into "painterly/atmospheric/cinematic-grade" and leaked the FH moody register → pouty/sullen Skeptic; corrected to bright/high-key. **§4 fully REVERSED** (flat-cel was wrong; the channel is semi-realistic in FIDELITY, bright/funky in REGISTER; differentiation from FH is register + cast, NOT art style). **Durable lesson graduated to canonical §8:** style_suffix is the highest-leverage lever on look — read it FIRST when renders look wrong; negative suffixes veto the model; register words drag the whole channel incl. faces.

**Also banked:** the two approved Skeptic/crew references (`02_egyptian_tomb`, `08_mughal_india_palace` lineage → `skeptic_ref.png`/`driver_ref.png`, semi-realistic) + the model bake-off verdict (NB2 default / NB-Pro hero-splurge / Seedream disqualified for character drift, conditional on those refs). Reference-render is LIVE, not "deferred." MERCATOR1 shipped on the interim painterly suffix (good enough, not re-rendered); ep4+ uses the bright suffix. Still open on Ep3: finish leg (kling 0 → `reassemble_static.py` + ffmpeg Ken-Burns strip), thumbnail, manual upload (category 27, Altered=Yes), `category_id` "24"→"27".

**AFTERNOON (same day) — the render-config gauntlet (four more fixes after the suffix reversal).** The §4 suffix swap fixed the cartoon but four more faults surfaced, all now banked to `_QQrew.md §4b`: (1) **interim→bright suffix** — the first swap over-corrected into "painterly/atmospheric/cinematic-grade" and leaked the FH moody register (dreary scenes + pouty Skeptic); corrected to bright/high-key/vibrant. (2) **ALL-NB2 standardisation** (QQrew-only) — dropped the flux/nano_banana model mixing; one model family (NB2 `/edit` for character, NB2 text for wides) for a consistent look; the optionality wasn't worth the friction. (3) **aspect-ratio bug** — NB2 wants `aspect_ratio` STRING, flux wants `image_size` DICT; wrong form → 1024² square, and `/edit` echoed the portrait `skeptic_ref.png` → portrait stills. Fixed per-model; NB2 rounds to ~1344-1376 wide so **`enforce_16x9.py` is the post-render pixel-exact pass** (or assemble scale-to-frame). (4) **the reference LOCK is the real lever for character beats** — `/edit` beats bypass the style_suffix; `REFERENCE_PROMPT_LOCK`+`TAIL` hardcoded "painterly rendered skin / warm cinematic lighting" → dreary+pouty Skeptic; de-mooded to bright + "never bored/pouty/flat." (5) Skeptic canon expression deadpan→"bright engaged, warm easy half-smile, sharp and lively." **Probe confirmed all four: bright, consistent NB2 look, 16:9, Skeptic smiling.** Ep3 close-out now also includes an `enforce_16x9.py` pass before assemble.

### 30 June 2026 (evening) — THE DE-BIAS + QQREW RENDER-UNBLOCK SESSION
Executed the long-deferred de-Final-Hours doc refactor and unblocked QQrew Ep3 — two threads that turned out to be the same disease (FH assumptions baked as universal, in docs AND code).

**The de-bias (modules, gated, one at a time per the patch's own warning).** (1) `_Final-Hours.md` v2 — absorbed ante-machinam's craft as a new §11 Craft Canon (retention canon, VISUAL patterns, beat-granularity + animatable-foreground RELABELLED from "Constitution physics" to FH craft — QQrew is the proof they're not physics); doc now self-contained. (2) Canonical v2 — added §2B ⚠ bias-warning block + the code-leakage checklist + `[CRAFT: FH-derived]` tags; rewrote §8 to mechanics-only (the real 5-item Constitution, the two-mode promotion rule, the script format, the author pre-flight, the wpm calibration); rescued the 3 orphan mechanics that lived ONLY in ante-machinam (silence-reconciliation, Mode-B promotion, author pre-flight); scrubbed all 6 live ante-machinam/machina refs. (3) `ante-machinam.md` RETIRED — tombstone header redirects to canonical §8 (mechanics) + `_Final-Hours.md §11` (craft); body preserved as historical record; ~13-doc footer scrub DEFERRED (tombstone makes it safe — backlog #35). (4) `_QQrew.md` repointed off ante-machinam + banked the night's two code-leaks. **Meta: BUILD ORDER ENCODES BIAS — enshrined in canonical §2B.**

**The QQrew Ep3 render-unblock (the diagnosis chain).** Ep3 had died at the stills leg on BOTH Mission Control and CLI — reproducible, so not a transient blip. Diagnosed via zero-spend `--storyboard-only` probes to TWO stacked config bugs: **(a)** the channel ran out of a `crew-wip/` staging folder, and `modea_leg._engine_project` basename-collapses the project path assuming `<channel>/projects/<slug>` → it invented `./<slug>/`, loaded the wrong empty folder, died before the first PNG (MC swallowed the error — P7 blindness confirmed again). **Fix: renamed `crew-wip/`→`qqrew/`** (honour the invariant) via `git mv` + safety-net `mv` for the untracked scp'd canon, headers + channel.json name updated, banked everywhere. **(b)** canon precedence — the engine loaded `base_canon` (driver-only) and ignored the project `canon.json`, so the Skeptic's 128 `{skeptic}` tags couldn't resolve. **Fix: merged the whole crew into `base_canon`** (the correct home for recurring cast). Ep3 then rendered 213 stills clean. **Both are code-leaks → canonical §2B + backlog #33/#34.**

**The catch at the gate (the win inside the disappointment).** The 213 stills came back as near-identical PHOTOREAL beauty-portraits — not the bright flat-cel curiosity-explainer. Caught on Peter's eye at the stills review, before a cent of animation. Diagnosed from the storyboard to TWO prompt-construction faults — channel `style_suffix` not appended (no "NOT photorealistic" → flux defaults photoreal), and the ~120-word beauty-canon eating the prompt (flux weights the front; the scene/posture arrived starved). **The script was vindicated** (the VISUAL lines had real variety — crouch-at-pelvis-diagram, air-quote, in-world numbers — they just never survived the render). Banked as `_QQrew.md §6a` doctrine (canon = a short identity tag, not a portrait — the character-channel inverse of FH place-canon) + `§11 P0` fix spec. Ep1-vs-Ep3 theory: Driver's shorter canon survived the latent fault; the Skeptic's lush portrait exposed it.

**Net:** the machine that was dead this morning now renders QQrew clean; the doc set is de-biased; the one open problem is scoped (prompt-construction, P0) with a probe-gate before spend. Worklog folded the whole @Q-Qrew arc in for the first time.

### 30 June 2026 (day) — @Q-Qrew Ep2 (ICE AGE) SHIP + Ep3 (PREGNANCY) BUILD
(Narrative: `SESSION_NOTES_2026-06-30.md`.) Shipped Ep2 "The Disturbing Things Humans Did to Survive the Ice Age" (207 beats static, category 27, Skeptic debuted in 13 reaction cutaways). Built Ep3 (pregnancy) — Skeptic's first SOLO-LEAD — with the headline craft finding: **the 55-word ceiling is a CEILING not a target** (the first draft ran 4-5× too long off the canonical's ceiling-bias; re-chopped to a measured ~5w/beat profile from the shipped scripts). Graduated the Skeptic voice+character canon to v3 (deadpan-scales-against-absurdity, deploys-specifics-as-authority, cracks-exactly-once); locked Zadie@0.95 (slow-down shifts TELLING→EXPLAINING). Crunchy-verified-data pass (every figure primary-sourced). Also fought a long render-debug saga banked as operational doctrine (one-job-at-a-time; gate-handshake fragility; `--unattended` reliability; MC swallows orchestrate stdout). Prepped the de-bias rewrite (gated on the current `__` doc text — executed THIS evening). The vanished-past reconstruction VEIN scoped (research-led unfilmable-past reconstruction; Maracanazo lead; test story-driven on the normal 48h bar). → `_QQrew.md`.

### 29 June 2026 — @Q-Qrew CHANNEL #12 LAUNCH (the anti-Final-Hours bet) + Ep1 (SOAP) SHIPPED
(Narrative: `qqrew/docs/SESSION_NOTES_QQrew_FULL_DAY.md` + `MASTER_SESSION_NOTES_crew.md`.) A whole new channel from frustration-with-Final-Hours → designed → cold-open proven → full episode → SHIPPED in one mammoth day. **The thesis working as designed: the production system is the moat; a stalled channel's answer is another cheap roll in an adjacent lane.** Born partly from a real wellbeing signal (the dark Final-Hours register was fatiguing) → build something BRIGHT. Locked: a recurring CREW (Driver/Skeptic/Brain) investigating the unfilmable; flat-cel illustration; Evan@1.05 dry-humour smirk; fast-cut static (the cut is the motion, Ken-Burns is noise over 200+ beats); the leanest config in the portfolio. **Ep1 "Humans Went 200,000 Years Without Soap"** — 238 beats, true-static 7:22, Driver-solo, Semmelweis as the 55-beat climax-spine. Peter's verdict: "very very very very good." **Doctrines banked → `_QQrew.md`:** length (fast-cut ≠ short — write MANY fast beats); the thumbnail formula (reaction-subject 9/10 expression, character-right text-left, no-echo, flat-saturated-pop background, margin_y:20, all-three-darkening-off, model-priors-not-promptable); the wide-angle establishing-still class; the audio-seesaw fix (audio-segment ≠ visual-beat → synthesise runs continuously, Whisper-map stills). The Final-Hours-bias meta-lesson surfaced here first (BUILD ORDER ENCODES BIAS) and drove tonight's de-bias. Naming logic: distinctiveness > sayability for a feed-discovered channel (@Q-Qrew = "lots of Questions" + Crew-on-a-Quest).

### 28 June 2026 — THE LAZARUS LAUNCH BUILD (bias rebuild + Yellow Wallpaper + the craft-doctrine harvest)
Relaunched Lazarus Films from a rejected render into a shipped first film, and in doing so converted a night of frame-by-frame wrestling into permanent craft doctrine. The headline isn't the video — it's that the control panel earned its place as the craft channel's primary path, and the session generated enough hard-won rules to fill a new `_Lazarus.md` §7.

**★ The candlelit-render rejection → total visual rebuild (→ `_Lazarus.md` §6, banked there this session).** The first render (Tell-Tale Heart, all-Kling, 4:31) came back period-gothic and candlelit; Peter rejected it outright ("I personally wouldn't watch this"). Diagnosed the root cause as a bias shared by BOTH the model and the assistant: a default toward darkness, period-gothic, chiaroscuro — because that's the training-median cliché AND because darkness conveniently hides model flaws. The correction is the **McEwan thesis: dread lives in the bright, fully-lit, present-day ordinary — terror in the normality, not the gaslight.** Locked across ~6 cheap stills-only tests (~$1 each), operator's eye as the gate ("I know it when I see it"). Folded into `_Lazarus.md` §6: modern-exposed base look, the dread-grade signature (cool/desaturated/sickly-green-grey/clinical), three named poles (Cement Garden / Prisoners-Deakins / Mayor of Easttown), object-anchor law (beat the period prior with visible modern OBJECTS, not adjectives), human-anguish face-open (open on the sufferer's face, reuse as thumbnail, then render faceless), anti-stock anguish-face recipe, banned-word list.

**★ Yellow Wallpaper SHIPPED (modernised, first-person).** Gilman's 1892 text reimagined as present-day postpartum confinement. Voice cast: **Rosalind / 0.85** — auditioned against Beatrice; chose vulnerable-part-of-the-story over abstracted-narrator (first-person unreliable narrator demands the listener be bonded TO her, not read to ABOUT her); British modern-not-posh; 0.85 the seam between 0.8 (drags depressive) and 0.9 (too in-control). Rendered through **Mission Control's stills-review gate** — every still reviewed/overridden frame-by-frame before any Kling spend, the first time the panel drove a real Lazarus launch. 25 beats, 7:33, banger anti-stock thumbnail (freckled/exhausted anguish face, "IT MOVES / AT NIGHT").

**★ The length miss (the cautionary tale, → §7.3).** First expansion was 18 beats / 355 words → rendered at ~2 minutes. "I thought we scripted for 10 minutes" met "it's 2 minutes." Root cause: **length comes from WORD COUNT, not beat count** — 355 words at ~120 wpm is ~2 min regardless of beat count. Fixed by expanding to ~1,180 words / 26 beats (restoring the descent-stages the compression dropped: denial, being-recorded, the meal-woman's distance, a four-stage descent, an expanded climax). Rule banked: target words = runtime × ~120, count words before rendering, never assume from beats.

**★ The image-reference investigation (→ §7.4).** Tested fal `flux-pro/v1.1/redux` end-to-end (endpoint real, takes `image_url` via `upload_file` — the `animate_still` pattern). Finding: **redux holds STYLE/family, not an exact PATTERN, and under-weights the prompt (echoes the source).** So it does NOT solve wallpaper-identity — and named-pattern prompting already gets same-family for free. Reserved redux/image-reference for the two things prompts CAN'T hold: character-face consistency and per-film look-lock — and even then with structural/identity conditioning, not redux. A deliberate future build, not a launch bolt-on. (En route, confirmed stills live under `<project>/modea/stills/`, and the standalone `stills` command needs `modea_beats.py` translation: parse→modea_beats→stills, → §7.6.)

**★ The unreliable-narrator reframe (→ §7.5).** Wallpaper drifted between beats (same sage-on-mustard family, different exact print). Resolved not by fighting it but by reading it: on a FIRST-PERSON account of a destabilising mind fixated on a wall, within-family pattern-drift reads as her perception, not a render error. **But story-specific** — only works because the narration is subjective enough to absorb it; on a composed/reliable narrator it would just read as a mistake. The general principle: continuity variance is an asset only when the narration is subjective enough to carry it.

**★ The detailed-prompt-by-default policy + hard-shot library (→ §7.1, §7.2).** A night of overrides crystallised into doctrine. Lazarus prompts are fully specified by default (the happy-accident luxury is for lower-craft channels; here the look IS the moat and a wrong default costs a re-still→re-animate→re-assemble). Every prompt carries **material + composition + negation**, specifying what the model defaults WRONG (not everything — over-specifying produces mush). Repeat-offender compositions + proven overrides banked as a library: faces-touching (hands + face-turned-away, never frontal); back-of-head-on-wall (uneven light, shallow DoF, "not a flat backdrop" — kills the Zoom-virtual-background look); macro-surface (name the material, negate gloss/wet/cells — kills the "jelly" read); wall-pattern-presence ("yellow wallpaper" → solid yellow wall unless the pattern is forced); almost-a-figure (suggestion woven into the print, never a literal blob); pattern-deforming (bulge over a hidden shape, not a tear-against-void). Plus the **named-William-Morris-pattern continuity lever** (name a specific real pattern, not "a pattern" — holds family across beats without an image reference).

**★ Mission Control earns its place (→ §7.6).** Peter's realization: the panel wasn't wasted work made redundant by batch+Studio-review — it operates at a different point. Studio review is post-hoc (money spent, frames animated); Mission Control is pre-spend (judge/fix stills before any Kling). Batch+Studio for volume channels; Mission Control for craft channels where one frame can break the piece. The tool maps to the channel's strategy. Consequence: the daemonization fix (`systemd-run --scope`) graduates from minor backlog to a real reliability need now the panel is Lazarus's primary path.

**Banked to backlog:** Mission Control drops music (#30); slow-fill clip-stretch (#31); Lazarus OAuth unwired (#32).

**The Saki / Dahl find (→ `_Lazarus.md` §5, PD-selection — to bank).** Traced Dahl's documented PD ancestors: Poe (direct, "The Landlady"), and — via Joyce Carol Oates placing Dahl in "the select society of Saki" — **Saki (H.H. Munro, d.1916, fully PD worldwide) is the closest thing to "public-domain Dahl"**: short, vicious, ironic, twist-in-the-tail, domestic. *Sredni Vashtar* (confined child, household, quiet horror — near-Cement-Garden) is a strong V2/V3. Also Grimm (Oates's "variants of Grimm"), Kipling, Dickens (*The Signal-Man*). Gives Lazarus TWO dread modes: slow-burn (Gilman/McEwan, Rosalind) and twist-in-the-tail (Saki/Dahl) — and **Beatrice** (the rejected Yellow-Wallpaper audition: rich/authoritative/graceful) is banked as the twist-in-the-tail narrator voice, since that mode WANTS the knowing, detached, observing register the first-person mode rejected.

**Durable lessons to graduate (DONE/PENDING):** the §6 visual doctrine + §7 craft block → `_Lazarus.md` (the §7 block written this session, to insert against the REAL file after reading it — assistant did NOT have the latest doctrine in context and refused to regenerate from memory); the Saki/Dahl two-mode finding + Beatrice → `_Lazarus.md` §5; the music-drop + fail-loud-assemble pattern → §6 reliability doctrine alongside #11/#30; the word-count-not-beat-count length math → ante-machinam §6 alongside the wpm calibration (#29b).

### 26–27 June 2026 — THE STAGING-TOOL SESSION + THE LEVELLING BATCH + THE FEATURE LAUNCH
Built the front door for the batch pipeline, used it to level the whole portfolio's runway in one staged run, then watched the two feature films go live and corrected a near-miss wrong conclusion about feature-length.

**★ `stage_batch.py` — SHIPPED (the zip-to-inbox front door).** Takes zip(s) of `<name>.md` + `<name>.thumb.json` pairs, runs on the BOX, and: auto-renames filenames to valid slugs (underscore/space/case → normalised, both pair members together); auto-fixes non-ASCII content (smart quotes, em-dashes → ASCII) in the staged copies; then runs the REAL gates — thumb schema (`subject`+`title`), `ingest.validate_slug`, `ingest._parse_header_channel` → `_resolve_channel_folder` (routes each pair to its channel by HEADER, not zip name), slug-collision vs existing projects, and `ingest.verify_beats` (the real `no_visual`/`wordless` check). Report-first (stages nothing); `--commit` routes clean pairs into `<channel>/batch_inbox/`. Output = per-channel staged-count table + rejected list. **Reuses the actual `ingest` functions, so a green report genuinely means the batch won't prep-fail — not a parallel reimplementation that can drift.** Multi-zip via `--zip a.zip b.zip` or `--zip-dir <folder>`; cross-zip duplicate-name clash guard. **Closes #11m (thumb-schema) and #11o (no_visual) as a pre-stage gate; it's the practical front-end to the §5.9 three-gate pre-flight.** Zip is throwaway transport (auto-fixes live only on the staged box copy; laptop source untouched).

**★ The 32-video levelling batch — staged clean, fired.** Worked back from a common **09 July** tail: each channel fills from the day after its OWN current scheduled tail (Soak 28 Jun → Dawn 01 Jul → Prehistoric 03 Jul → YHTBT 08 Jul → Cathedral 09 Jul), plus Scripture's `jericho` filling the single 04 Jul internal gap (the hole jacob-esau left). 10 July becomes the first empty day → the NEXT batch's shared `publish_start`. `stage_batch` walked a genuinely messy input (duplicate zips, a split Soak across 2 zips, Mac `.DS_Store`, a missing thumb, 3 already-built collisions) all the way to "7 zips, 32 pairs, ALL CLEAN, ZERO REJECTS" — every problem it flagged was real, no false positives. Dry-run 6 ok / 32 planned; fired in tmux.

**★ The full eleven-channel dump is clean.** `dump_channel.py --all` now sweeps all ten live-token channels (Success Coach excluded — see #11p). The two new channels resolved correctly: **Lazarus Films** (0 videos, designed-not-shipped) and **Woodworking for Everyone** (34-video back-catalogue, 0 scheduled). Runway read captured: Final Hours + Scripture deepest (09 Jul), Sacred Soak was dry next day (the levelling trigger).

**Banked lessons:**
- **Dead OAuth token crashes headless `--all` (→ #11p, NEW).** Success Coach's `peteralkema6@` token (`invalid_grant`) tried to launch a browser in the headless dump and crashed the whole sweep. Excluded as interim; real fix is skip-on-bad-token. The canonical claims a guard exists — it doesn't.
- **`--zip-dir` reads EVERYTHING in the folder.** Re-exporting a channel zip under a NEW name (`dawn26june.zip` + `dawn26junenew.zip`) leaves both → the clash guard correctly rejects the doubled scripts, but it's noise. Rule: overwrite the same filename or wipe the staging folder before re-rsync. The clash guard is what made this safe (caught a mis-export, never silently double-staged). A channel's content MAY be intentionally split across multiple zips (Soak's 12 came as 2 non-overlapping halves) — both wanted; the guard only fires on the same stem in two zips.
- **`stage_batch`'s real-content proof:** validated against stubs in the sandbox, then confirmed live — `from mission_control import ingest` resolved (`ingest ok: True True True True`), and it caught real defects on real scripts at zero spend.
- **Channel docs now carry a no-duplicate shipped ledger.** All five levelling channels' doctrine docs (`_Sacred-Dawn`, `_Prehistoric-Disasters`, `_Cathedral-of-Stars`, `_you-had-to-be-there`, `_Scripture-On-Screen`) updated with a dated "26 June levelling batch — shipped" record so the 20 shipped titles aren't re-authored; Sacred Dawn's held-backlog pruned of the now-shipped items (Azazel/Bloodline/Garden's-Other-Tree/Enoch-throne); jacob-esau marked abandoned. `_Woodworking.md` doctrine doc added.

**★ The two FEATURE FILMS went live (27 June) + the analytics correction.** Both published Sat 27 June 01:00 CEST, public, thumbnails attached. The early read nearly produced a wrong call: at 8h the impressions looked dead (Watchers 18, Revelation 50, ~0 CTR) → "feature-length doesn't distribute." Two corrections killed that conclusion: the impression curve is a **delayed hockey-stick** (flat 0–8h, sharp ramp after — Revelation 50→148 in ~90 min), and **measured against its own channel Revelation is the #1 recent video by views** (5 vs 3/3/1/0/0…). So the constraint is **channel-level distribution, not format** — Scripture On Screen isn't distributing ANY format yet. The feature-length question narrows to **long-form AVD** (Revelation 3:18 on 69 min at n=5 — too small to trust, the number to watch at 48h). Thumbnail A/B tests set up on both (motion-still variants vs originals, via `make_thumbnail.py --still`), running in Test & Compare — but starved by the same low impressions, won't resolve for weeks.

**Banked lessons (analytics):**
- **Don't read long-form at 8 hours.** Feature-length has a distribution ramp a short doesn't; an 8h read nearly killed the format on a false signal. Read long-form later.
- **Compare against the OWN channel's cohort before judging the format.** A feature that looks dead absolutely can be the channel's top performer. The variable was the channel, not the runtime.
- **Channel distribution is the real frontier.** The pipeline produces faster than the young channels grow an audience — the next strategic lever is channel-level reach, not more content or better thumbnails alone.

 → canonical §5 (it's now the standing first step before any batch, ahead of the §5.9 three-gate pre-flight); the dead-token skip-guard requirement → §6 reliability doctrine alongside #11; the multi-zip / overwrite-or-wipe staging discipline → §12. **PLUS the 27-June analytics lessons (→ §9 analytics laws): (a) long-form has a delayed distribution RAMP — an 8-hour read on a 70-min asset is unreliable and can produce a false "doesn't distribute" verdict; read long-form later than a short. (b) Always compare a video against its OWN channel's recent cohort before judging the FORMAT — a feature that looks dead in absolute terms can be the channel's top performer; the constraint was channel-level distribution, not feature-length. (c) Channel-level distribution is the live frontier — the pipeline produces faster than the young channels grow an audience to receive it.**

**A throwaway helper worth remembering:** built a contact-sheet generator (PIL, no ImageMagick on box) — tiles a project's `modea/stills/shot_*.png` into one labelled grid JPG (~1.7 MB for 360 stills) so you pick a thumbnail frame by scanning one image + noting shot numbers, instead of pulling ~86 MB of PNGs. The pick → `make_thumbnail.py --still modea/stills/shot_NNN.png` bakes the headline on. Used for both feature A/B variants. *(Candidate to promote to a `make_contact_sheet.py` shared tool if thumbnail-frame-picking recurs.)*


### 23 June 2026 — THE 55-VIDEO BATCH-OF-BATCHES + THE FIRST FEATURE FILMS
The biggest production day to date. The factory ran six channels and 55 videos in one unattended sweep, then rendered its first two feature-length films end-to-end — proving the production system scales an order of magnitude with no format-level break.

**The 55-video sweep (one `run_all_batches.py`, `batch_plan.json`-driven).** prehistoric (10), sacred-soak (5), you-had-to-be-there (10, kling 0), cathedral (10), scripture (10), final-hours (10). Each channel's `publish_start` set the day after its furthest-future scheduled tail (via `dump_channel.py --scheduled-only-summary`), 24h interval, kling 2 except YHTBT 0. **54/55 shipped clean, cadence-continuous, zero gaps** across a ~21.4h run. Scheduler proven at portfolio scale; day-after-tail is the no-state collision rule.

**The three-gate zero-spend pre-flight (standing practice → #11m, §5.9).** (1) `parse_script.py` every `.md`; (2) slug-collision scan; (3) `run_all_batches --plan`. NB the deep `create_project` verify is a FOURTH gate the cheap three don't cover (the feature `no_visual` catch).

**Cathedral thumbnail-rescue (caught at the dry run, zero spend).** 10 thumbs present + paired but missing `subject` → all 10 `prep_failed`. Authored the 10 `subject` prompts from Cathedral §7, preserved `title`/`subtitle`, re-staged clean. → #11m. The `NN_`→`NN-` slug trap caught a second time (#29).

**The `jacob-esau` straggler (isolation working as designed).** Failed at the STILLS leg (vo + publish.json, no final_video, 0 stills → transient fal blip). The per-channel `try/except` isolated it; the batch rolled on. Resume via `orchestrate.py`, slot (03 July) intact. **Second data point (after `lost-human-species`/Inworld) that the durable fix is generic retry-with-backoff across ALL APIs → #11.**

**★★ THE FIRST FEATURE FILMS (the headline).** `watchers-the-movie` (sacred-dawn, 360 beats) + `revelation-the-movie` (scripture, 355 beats), ~12,400 words each, both `--kling-count 40` front-loaded (~$17 fal each), scheduled **Sat 27 June 01:00 CEST** (Fri 19:00 US prime — a feature is a weekend long-watch EVENT, slot hand-set, not day-after-tail). Run as a two-channel `run_all_batches` (sequential, per-channel isolation). **Both rendered clean + uploaded.** Confirmed COMPLETE not truncated: **video-duration == voiceover-duration to the half-second** (watchers 4490.5s vid / 4490.6s voice; revelation 4183.2s / 4183.3s). The pipeline spans 8-min shorts to 70-min features with no break — direct proof of the production-system-is-the-moat bet.
  - **The `no_visual` prep-failure + recovery (→ #11o).** First run, watchers `prep_failed`: `verify={'ok': False, 'no_visual': [0,2,4,5,51,56,69,...]}` — 22 of 360 beats had narration but no `VISUAL:`. `parse_script.py` and `--plan` BOTH passed it; only `create_project`'s verify checks. Caught at ZERO spend. Fixed by authoring the 22 visuals (`movies2`), verified locally (0 missing) before re-staging. Open: were they missed, or meant to hold the prior image (→ an inherit-visual rule)?
  - **Runtime came ~30 min UNDER the ~103-min estimate — NOT short, the estimator is wrong (→ #29b).** Both ~70 min (74.8 / 69.7), consistent with each other (the tell it's systematic). Measured 165.6 / 178.2 wpm vs the formula's 120 floor. Calibration banked.

**Banked to backlog:** phone-first logging rewrite (#11l — scrap ASCII-art + `phaseStrip`; shared emitter + context across four layers); `--plan` thumb-schema (#11m); `--plan` `create_project`/`no_visual` verify (#11o); auto-archive re-confirmed (#11n); faster-encode promoted (#19); generic API retry-with-backoff (#11).

**Durable lessons to graduate (pending the canonical edit this session):** three-gate pre-flight + the `create_project`-verify fourth gate → §5.9/§12; the wpm calibration → ante-machinam §6; feature-length proof (system scales to 70 min) → §9/§5; generic-retry-with-backoff as reliability doctrine → §6/§12; the `NN_`→`NN-` slug-prefix rule → §12.

### 21 June 2026 (evening) — THE SACRED SOAK LAUNCH
Stood up **Sacred Soak** (`@SacredSoak`), shipped Vol. 1 (33:28 Book-of-Enoch, 119 beats, Elliot). The thumbnail anchor doctrine (→ §7c + §9 #20): top-left default, bottom-left on collision — per-IMAGE; render both, Sonnet judges collision + legibility, fail to top-left on error. Kling-for-a-soak law (§12.3): gentle drift is free Ken-Burns. **★ THE ANTON FIX** (§6 + §12): no `shared/fonts/` on the box → silent DejaVu fallback portfolio-wide; commit `Anton-Regular.ttf` with `git add -f`. `make_thumb_bottomleft.py` gained `--title2`.

### 21 June 2026 — THE READ-SIDE SESSION
Built `dump_channel.py` — read-only metadata mirror, `--scheduled-only-summary` + `--cadence`. Caught the Lady Be Good orphan + a collision. → §5.10. Banked scale-vs-craft → §2A; analytics laws → §9 #14–#19; Amsterdam-time/golden-hour/Artlist gotchas → §12.

### 20 June 2026 — THE SATAN FLAGSHIP SESSION
Worst video (15.7% AVD) → strongest film. Flagship-rebuild method (§2b/§6b): worst-but-well-clicked = best raw material. MUSIC_LEVEL 0.07→0.040 via LUFS (measure the bed STEM at applied gain vs voice, not the final mix). Bottom-left flagship thumbnail (§7). Chapters from `audio_start`, never estimated. → #11b–#11f.

### 19 June 2026 — the batch-of-batches, music across four channels, thumbnail word-fit
Built `run_all_batches.py`. Music onto four channels (box-local). Thumbnail word-break fixed (`break_long_words=False`). First multi-channel scheduled run. Banked: slug rule; `--plan` doesn't catch slug errors; the `+02:00` summer trap; the render-vs-publish race. → §5.9, §6, §12.

### 17 June 2026 (evening) — audio-chain fixes, Prehistoric live, competitor benchmark
Three audio-chain bugs fixed (undefined `channel_dir`; `normalize=0`; per-channel `speakingRate`, 0.9). Toba + Chicxulub published. Wild Horizons benchmark (~218K floor, ~32-min avg → long-form). Quota now ~100/day. → §6, §9, §12.

### 17 June 2026 (day) — the Prehistoric Disasters channel, end to end
A channel from zero + three permanent pieces: thumbnail pipeline (Flux N=2 → Sonnet → locked overlay, scrim-not-global-darkening); unattended batch runner (`auto` gate + `--unattended` + `run_batch.py`); music into convergence (curated `--music-dir`). → §5.8, §6, §9.

### 16 June 2026 — motion root-fix + upload live across five channels
Fixed `default_motion` at source. Upload working (gustloff `wiykuEhTY1k`); OAuth → Production; five channels authed. → §5.3, §5.5, §5.7, §6, §12.

### 15 June 2026 — Mission Control v1.0 → v1.8
Nine patches. Audio→stills seam; the drift fix; panel persistence; reliability trio. → §5.6, §5.7.

### 13–15 June 2026 — Mission Control build arc + TIERED RENDER
TIERED RENDER proven on Enoch: Kling front-N / free Ken-Burns floor, ~$16.80/video at N=40. → §5.1–§5.4, §9.10.

### 12 June 2026 — Sacred Dawn ep2 + browser-pipeline decision
Ep2 (184 beats). Banked: `search_videos` corrupted — use `search_niche_finder_channels`.

### 8–9 June 2026 — You Had To Be There launch + pipeline hardening
Channel launched. Decade look-override Phase 1. Banked: un-filmable vs re-watchable; served vs searched; title↔thumbnail complement. → §9.

### 6–7 June 2026 — orchestrator + first-principles audio reset + second channel + music
Built `orchestrate.py`. One continuous voice track. Banked: resolve-identity-explicitly-fail-loudly.

### 5 June 2026 — Synthetic Press dual-mode pipeline
Built + box-proved the dual-mode pipeline.

### 3 June 2026 — Final Hours #7 + stills-review infrastructure
Shipped FH#7. Banked: Flux silent safety-reject (`safety_tolerance:"5"`); Override > Notes. → §6, §12.

---

_Older sessions (pre-3 June) live in the canonical reference and ante-machinam where they were absorbed._

- 2 Aug: fix-commit -- kb-tail continuation grammar (seam PSNR 9.6 -> 35+, direction-aware, no cap-freeze) + fx speckles (channel-config, uniform, inert default; Filmora verdict productionized). visuals.py only.
