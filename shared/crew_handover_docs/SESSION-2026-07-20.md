# SESSION NOTES — 2026-07-20 · Bentley & Watson "Roadster" film + build_lego

## ⏭ NEXT SESSION — START HERE

**State:** Act 1 fully authored (40 phenomenon + wall-to-wall narration) and
gate-clean. Act 1 grid render was fired (`build_lego stills`, ~$8.80) into
`bentley-and-watson/projects/bw-space/grid-b01/`. Everything committed to git.

**Immediate next actions, in order:**
1. **Review the Act 1 grid + PICK.** Open `projects/bw-space/grid-b01/`, promote
   ONE winner per beat to `shot_NNN.png`. Watch for flux-fallback variants
   (softer, possible stray logo) on the 7 EXT2 two-ref beats (10,21,22,23,24,25,27)
   — skip those, re-run `stills` to refill if a beat's clean variants are weak.
2. **Add a `.gitignore`** (repo reads as a wall of untracked `clips/`,
   `thumb_candidates/`, `stills/`, `grid-*/`). Ignore render outputs; KEEP
   `master.csv`, code, docs, `_skip.png`. This makes `git status` readable again.
3. **Build `build_lego clips`** — the last verb: reads picked `shot_NNN.png` +
   the CSV's `air`/`move`/`motion`, calls the engine's animate/ken-burns. Closes
   the CSV->clips half.
4. **Author Acts 2 & 3** (phenomenon + wall-to-wall narration) against the
   proven Act-1 pattern; gate; then `audio` -> `calibrate` the whole 120-beat film.
5. **Apply `patch_lego_refmode.py`** to `shared/docs/_LEGO.md` (laptop->git->box)
   and delete the stale untracked `sacred-dawn/projects/enoch-moon/_LEGO.md`
   duplicate so there's one source of truth.

**Open decision (film):** ending stays REAL drift; final frame is the ambiguous
Watson-asleep-at-home shot (006 from probe 1) — NOT "it was all a dream." Held one
beat, no narration, unresolved. Real-or-dreamed left to the audience.

---

## WHAT SHIPPED THIS SESSION

**`build_lego.py`** — new channel-agnostic tool, SUPERSEDES `build_moon.py`.
Verbs: `normalise` `sweep` `film` `blocks` `stills` `audio` `calibrate`.
Config-driven (canon<-channel.json, banned<-rulebook.json, tts<-tts_provider).
CSV at `projects/<name>/master.csv`. `stills` = ref-aware 4x variant grid
(the piece the doc specified but no code implemented for reference mode).
Committed + on box.

**channel.json (Bentley)** — added film canon tokens (`ext/int/starman/void/
spectacle/mirror/home/wish/overhead`) + `{car}`/`{boot}` reference plates +
opaque `{starman}` visor. Reference plates built from the REAL 2008 Roadster
(public-domain Starman image), logos scrubbed: `roadster_ref.png` (identity,
all angles via text) + `roadster_boot.png` (the frunk cavity).

**Act 1 (bw-space master.csv)** — 40 phenomenon prompts (ref-discipline: 33
single-ref, 7 two-ref, zero three-ref) + wall-to-wall narration (511 words /
~214s, slightly heavy so calibration trims not pads). Gate: BANNED none,
tokenless none, unlit none. Committed to git (film-as-data, versioned).

**Two render probes ($0.80 + $0.64)** proved: dog identity holds behind glass;
`{home}` grade reads warm/distinct; the HAND renders (5 clean gloved fingers);
opaque visor fixed the face-bleed; `{car}` plate kills exterior drift and
extrapolates overhead from a side plate; multi-ref fragility quantified.

**_LEGO.md** — `patch_lego_refmode.py` written (adds §14 Reference Mode, §15
Mode Overrides, §16 What LEGO is NOT For, ledger). NOT YET APPLIED to
`shared/docs/_LEGO.md` — apply next session.

## KEY DECISIONS / STRATEGY
- Portfolio: 3 movie channels active, rest low-effort forward-batched. If forced
  to pick one: **Sacred Dawn** (nearest to monetization eligibility). Eligibility
  = near-term motivator; **browse/suggested distribution activation** = the real
  breakout meter. Pre-committed trigger: first channel to show sustained browse +
  held retention gets 80% of output for 8 weeks, no re-litigation.
- Bentley film is a festival entry (AAIFF, deadline 15 Aug) AND the channel's
  launch video — kill the channel if it flops.

## KNOWN REPO HOUSEKEEPING (deliberate, later — never `git add -A`)
- No `.gitignore` — render outputs listed as untracked everywhere (noise, not damage).
- Untracked real work to commit: `shared/crew_handover_docs/`, lazarus looktests,
  flywheel-progress-summary.md, enoch-moon notes.
- Untracked to gitignore/delete: `pipeline/`, `soap-full_*_from_box` dumps,
  `success-coach/new_thumbs/`.
- Stale duplicate `sacred-dawn/projects/enoch-moon/_LEGO.md` — canonical is
  `shared/docs/_LEGO.md` (tracked). Delete the duplicate.
