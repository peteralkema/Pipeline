# Machina — RETIRED

*This document is retired. It was the terminal-era operational playbook — the 12-step command card, the full per-command lifecycle, the orchestrator build status — and **Mission Control obsoleted all of it.** The production arc now runs from the browser console; there are no command cards to follow.*

**Where its content went:**
- The **architecture** (one machine / many channels, the `channel.json` identity contract, adding a channel, the per-film look override, and the *resolve-identity-explicitly-fail-loudly* design law) → `_YOUTUBE-MEDIA-FLYWHEEL-canonical-reference.md` §5.
- The **operator surface** (how you actually run a video now) → the canonical reference §5.1 (Mission Control) and §13 (the few surviving box commands until they become buttons).
- The **authoring craft** (Mode A/B, beat granularity, the VISUAL line) → `_ante-machinam.md`.

**Load these two instead:** `_YOUTUBE-MEDIA-FLYWHEEL-canonical-reference.md` (the system) and `_ante-machinam.md` (the craft). They are the whole doc set now.
